# S08 验证

日期：2026-09-20

## 命令

```
flutter test test/media_library_folder_walk_test.dart test/search_playback_queue_test.dart test/media_library_search_query_test.dart test/media_library_search_prompt_test.dart test/media_library_locate_button_test.dart test/media_library_list_tile_responsive_test.dart
```

结果：exit 0，+35。

`media_library_selection_drop_targets_test.dart` 中四条文案仍失败（S00 已记录），未当作本阶段回归通过。

## 结论

- 目录页标题为可点「媒体库 > … > 当前」；窄屏中间祖先进「…」菜单；搜索结果不显示面包屑。
- 「媒体库」`popUntil` 到根并切文件夹入口，不再堆 HomeScreen。祖先用 `pushReplacement` 并 reveal。
- 「包含子文件夹内容」默认关；会话内按目录记忆；跨启动只恢复最后目录开关。
- DFS 按 children 顺序展开媒体，循环/回收安全，不改 childrenIds。相对路径用库内目录名。扁平列表禁止重排/拖放索引。播放仍 `prepareLibraryPlayback`。
- 搜索匹配语义未改。

## 未跑

- 真机窄屏面包屑、开关后滚动位置、搜索→定位→返回整条手势
- 整库 `flutter test`
- 既有 drop-targets 四文案失败未修
