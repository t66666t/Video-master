# S05 验证

日期：2026-09-19

## 命令

```
flutter test test/library_activity_projection_test.dart test/media_library_recent_view_test.dart test/media_library_navigation_shell_test.dart test/search_playback_queue_test.dart test/media_library_grid_card_test.dart test/media_library_list_tile_responsive_test.dart test/media_library_top_bar_import_progress_test.dart
```

结果：exit 0，+40。

## 结论

- `recentAddedEntries()` 按批次 startedAt 倒序（同时间比 ID），成员保持导入序列；不按 lastUpdated。
- 单媒体批次直接当媒体；多媒体批次为「标题·当前N项」，全不可见则不留空组。
- 无批次但有 addedAt 的项单列；未知时间收进「更早添加」。
- 最近添加已挂到 HomeScreen；继续学习仍不可点。
- 打开走 `prepareLibraryPlayback`（search_playback_queue_test 仍通过）。
- 文件夹/压缩包/B站/YT-DLP 完成通知带「查看」，落盘失败时不发该成功条。
- 百项批次折叠时不构建成员卡片。

## 未跑

- 真机后台导入不抢滚动、旋转保留
- 整库 `flutter test`
- 既有 drop-targets 四文案失败未修
