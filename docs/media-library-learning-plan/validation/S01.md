# S01 验证记录

- 阶段：S01 活动数据 / 兼容迁移 / 唯一持久化
- 日期：2026-09-19
- HEAD：`0dd61c40`。未 reset/clean；未提交。
- 工具链：沿用 S00（Flutter 3.44.2 / Dart 3.12.2）。

## 本阶段行为与文件

新增独立活动区，写入原 `library.json` 顶层 `activity`（子版本 1）。不改 `VideoItem.toJson`，不新建数据库。

- `lib/models/library_activity.dart`：`LibraryActivityStore`、`MediaActivityRecord`、`ImportBatchRecord`、`LibraryImportSourceKind`
- `lib/services/library_activity_projection.dart`：可见性（含回收祖先、防循环）、最近添加分区、续学分组
- `lib/services/library_service.dart`：加载/保存挂钩、删除清理、公开 API
- `test/library_activity_storage_test.dart`
- `test/library_activity_projection_test.dart`

写入口仍是 `_writeLibrarySnapshot` → `_performLibrarySnapshotWrite`。未知未来 `activity.version` 原样回写。开放批次只在 `completeImportBatch` 落盘；`abortImportBatch` 不留批次。`updateVideoProgress` 不改活动；`recordValidPlaybackProgress` 只走现有节流保存、不 `notifyListeners`。

## 测试实际执行

```
flutter test test/library_activity_storage_test.dart test/library_activity_projection_test.dart test/library_persistence_recovery_test.dart test/library_subtitle_schema_v2_migration_test.dart
```

- exit code：0
- +19，All tests passed

覆盖：旧库不虚构 addedAt；往返且 VideoItem JSON 无活动字段；重复加载不造时间；更名/进度不改 addedAt；软删保留固定、永久删清引用；回收祖先过滤；坏条目隔离；未来版本不空写；取消批次不持久化；保存失败内存一致且重试落盘；完成标记不被进度归零抹掉；投影循环终止与续学分组。

既有 `library_persistence_recovery_test` / `library_subtitle_schema_v2_migration_test` 本轮复跑通过。

## NOT RUN

- 整库 `flutter test`
- S00 的 drop-targets 四失败未再跑（仍视为既有）
- 真机导入/播放采样（S02/S06）
- Settings 界面偏好（留给导航壳）

## 下一阶段须知

S02 在真实入库点调用 `beginImportBatch` / `noteImportedMedia` / `completeImportBatch` 或 `abortImportBatch`。复用 ID 不要覆盖 `addedAt`。分享两处 UI 监听不得各记一批次。
