# S00 验证记录

- 阶段：S00 基线核对（不改业务代码）
- 日期：2026-09-19
- 工作区：HEAD `0dd61c40`（4.4.1）；`git status --short` 与 `git diff --stat` 均为空。`git log -5`：`0dd61c40` 4.4.1 → `61018752` Fluent player 4.4 → `08e54250` 4.3 → `89f04f0c` 4.2 → `09504c4a` 4.1。
- 对照计划基线：`61018752`。`0dd61c40` 仅改 `docs/media-library-learning-plan/**`（含 zip），无 lib/test 业务 diff。媒体库源码仍对齐 4.4。
- 工具链：Flutter 3.44.2 stable，Dart 3.12.2（`flutter --version`）。

## 本阶段行为

只核对符号与跑指定测试。未改 `lib/`、`test/`。无人工截图。

## 源码已确认（rg + 短片段）

| 计划项 | 结论 | 定位 |
|---|---|---|
| 三个入口 / 最近添加 / 继续学习 / LibraryActivity / addedAtMs | 仍缺失 | 全库 dart 无匹配 |
| 活动批次 / 固定 / 隐藏 | 仍缺失 | 同上 |
| 搜索入口 | 4.4 提示框保留 | `showMediaLibrarySearchPrompt`：`lib/widgets/media_library_search_prompt.dart:8`；Home/Collection `_openSearch` 再 `buildMediaLibrarySearchResultsRoute` + `CollectionScreen.search` |
| 搜索队列 | 原父目录 | Collection `891:899`、Home `798:804` → `PlaylistManager.prepareLibraryPlayback` |
| 紧凑顶栏 | 辅助组件，无整页 AppBar 类 | `useCompactMediaLibraryTopBar` + Title/Icon/More；文件 `media_library_compact_app_bar.dart` 无 `MediaLibraryCompactAppBar` |
| 目录标题 | 仅当前名，无媒体库面包屑 | Collection `MediaLibraryCompactTitle(text: collection.name)` ~1488 |
| 包含子文件夹 | 仍缺失 | Home/Collection 浏览走 `library.getContents`；breadcrumb 仅 FluentPack 选择树 |
| 快照 | schema 2；tmp/bak/rename | `_currentLibrarySchemaVersion = 2`；`_performLibrarySnapshotWrite` `1413:1443`；payload 仅 collections/videos/rootChildrenIds/schemaVersion |
| VideoItem | 无活动字段 | `toJson` 有 lastPositionMs/lastUpdated 等，无 addedAt/播放活动 |
| 结构化导入 | 成员列表有、活动批次无 | `StructuredImportExecutionResult.importedVideoIds` `106:121`；`_importDirectoryTreeIntoLibrary`；`_runExclusiveImport` `1816`；`_rollbackStructuredImportState` `2355`；`addSingleVideo(reuseExistingItem:)` |
| FluentPack | 迁入在、活动语义无 | `importPortablePackageDirectory` `1726`；`applyPortableMediaMetadata` 恢复 lastPosition 等，不迁批次/固定 |
| 完成 | 确认后存末尾再归零 | `_handlePlaybackCompleted` `6314+`：`isConfirmedPlaybackCompletion` → `_saveCurrentProgress` → `saveProgressImmediately(..., Duration.zero)` |
| 失联 | 标记有、改 path API 无 | `isSourceMissing` getter；`markCurrentSourceMissing` `4911`；无 relocate/repoint path API |
| 布局/封面 | 复用 | `MediaLibraryLayoutDefaults`、`MediaLibraryGridCard`、`MediaLibraryListTile`、`FolderPlaceholderCover`、`coverLabel` |
| 导入表/快捷键 | 复用 | `VideoActionButtons.showImportMenu`；`DesktopMediaManagementShortcutAction` |
| AGENTS.md | 无 | 仓库 glob 0 |

分享双入口（源码确认，行为未跑）：

- 根：`main.dart` ~694 `IncomingShareListener` → `processIncomingSharedItems(..., requireCurrentRoute: false)`，去重 `_recentSignatures`。
- Home：未 `returnToSearchResults` 时 `_setupIncomingMediaHandling` `1022:1090`，独立 EventChannel + `_handledIncomingMediaSignatures`，默认 `requireCurrentRoute: true`。
- 两套去重不共享。Home 在前台时同一分享可能被两处各调一次入库。S02 批次只许登记在实际入库点。

## 测试实际执行

命令：

```
flutter test test/search_playback_queue_test.dart test/media_library_layout_profile_test.dart test/media_library_grid_card_test.dart test/media_library_selection_drop_targets_test.dart test/media_library_search_prompt_test.dart test/media_library_overlay_identity_test.dart
```

- exit code：1
- 合计：+27 -4（约 12s）

通过（本命令范围内）：

- `search_playback_queue_test.dart`：搜索打开保持原文件夹顺序
- `media_library_layout_profile_test.dart`：间距/滑条/列数/横竖屏样式等
- `media_library_grid_card_test.dart`：关闭默认水波纹
- `media_library_search_prompt_test.dart`：trim、间距、Esc、键盘、手机高度
- `media_library_overlay_identity_test.dart`：overlay key（输出计入 +27，无单独失败行）
- drop targets 中：resolve 选区、大号字体不溢出、点击两半、未选中不触发、拖拽带索引

失败（既有，非本计划；S00 未修）：

`test/media_library_selection_drop_targets_test.dart` 四处 `find.text` 精确匹配 0：

1. 手机文件夹顶栏：期望「上一级」
2. 手机最外层：期望「移入回收站」
3. 桌面文件夹：期望「移动到上一级」
4. 桌面最外层：期望「移入回收站」

源码：投放区 `label` 走 `DesktopMediaManagementShortcuts.buildTooltip`，非空快捷键时为 `标签 (快捷键)`。测试找裸标签。属基线失败，不能算本计划引入。

## NOT RUN

- 整库 `flutter test`
- 真机/模拟器：三入口、导入批次、分享是否双触发、失联重定位、播放完成续学
- `playback_behavior_policy_test.dart`（完成确认有测试文件，本阶段未跑）
- 应用启动、导入、FluentPack 往返

不得写“所有现有功能已验证”。

## 下一阶段须知

S01 可在 schema 2 快照旁加独立版本活动区；写入口仍 `LibraryService._writeLibrarySnapshot` / `_performLibrarySnapshotWrite`。不要写进 `VideoItem.toJson`。无活动模型可复用。界面偏好走 SettingsService 注册项。分享去重与批次登记留 S02。
