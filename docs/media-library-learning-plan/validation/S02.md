# S02 验证记录

- 阶段：S02 本地导入批次
- 日期：2026-09-19
- HEAD：`0dd61c40`。未提交。中途曾误用 PowerShell 写回损坏 `library_service.dart`，已 `git checkout` 该文件后用 UTF-8 重打 S01+S02 改动。

## 接入位置（只在 LibraryService 插入点记账）

- 多选文件：`importVideosBackground` → `LibraryImportSourceKind.localFile`；相册/文件选择/拖拽媒体/`processIncomingSharedItems` 仍只调用此函数，UI 监听器不另开批次。
- 目录：`importFolderSelection` → `_importDirectoryTreeIntoLibrary(..., folder)`
- 压缩包：`importArchiveSelection` → 同上 `archive`
- 字幕配对合并：`addSingleVideo` 无 batchId 时发单条批次；`removeSingleVideo` 清引用
- FluentPack 目录导入本阶段不传 `activitySourceKind`（留给 S03）

独占锁拒绝不 begin。回滚走既有 `_rollbackStructuredImportState` 并 `abortImportBatch`。addedAt 与新媒体同一次 `_saveLibrary`。后处理/缩略图不再开第二批。

## 测试实际执行

```
flutter test test/library_local_import_activity_test.dart test/incoming_share_signatures_test.dart
```

exit 0。另：S01 存储/投影/持久化恢复、folder scanner、archive safety、private import setting、schema v2 此前同会话复跑通过。

真实系统分享双监听：NOT RUN。批次 API 对同一 mediaId 在同一开放批次内幂等；未删除 Home 与 IncomingShareListener。

## NOT RUN

- 整库测试、真机分享/拖拽、S00 drop-targets 四失败
- 相册/文件选择器 UI

## 下一阶段须知

S03 给 `importPortablePackageDirectory`、B 站在线卡、YT-DLP 入库传独立 `LibraryImportSourceKind`，复用本阶段插入点，不要再改本地文件管线。
