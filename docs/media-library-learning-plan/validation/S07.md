# S07 验证

日期：2026-09-20

## 命令

```
flutter test test/library_activity_projection_test.dart test/media_library_continue_view_test.dart
```

结果：exit 0，+13。

```
flutter test test/media_library_recent_view_test.dart test/media_library_navigation_shell_test.dart test/library_watch_meter_test.dart test/search_playback_queue_test.dart test/media_library_list_tile_responsive_test.dart test/media_library_grid_card_test.dart
```

结果：exit 0，+42。S06 计量规则未改，watch meter 仍通过。打开继续学习条目仍走 HomeScreen `_openPlaybackScreen` → `prepareLibraryPlayback`。

## 结论

- 继续学习已挂到根页；空态说明播放后出现，并提供前往最近添加/文件夹。
- 投影：入列看 `lastPlayedAt`；旧进度无观看时钟进「之前未看完」；仅预览累计不计。同父目录折叠（最新在上，可展开），根目录散片分开。同名不同 ID 不合并。
- 固定媒体不在下方重复；固定目录不隐藏子项。卡片更多菜单可固定/取消固定；媒体可「不再显示」（不停止播放，并 `noteLibraryMediaHidden`）。
- 组次序在页内冻结，返回当前路由后重排。迷你播放卡底垫仍用既有 overlay。
- 回收/祖先回收仍由既有投影过滤。

## 未跑

- 真机 A/B 目录续看、横竖屏遮挡、重启后固定
- 整库 `flutter test`
- 既有 drop-targets 四文案失败未修
