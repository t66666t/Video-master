# S04 验证

日期：2026-09-19

## 命令

```
flutter test test/media_library_navigation_shell_test.dart test/media_library_overlay_identity_test.dart test/media_library_compact_app_bar_test.dart test/media_library_search_prompt_test.dart
```

结果：exit 0，合计 +23（其中 `media_library_navigation_shell_test.dart` 单独为 +12）。

## 结论

- 根页标题区换成轻量「继续学习 / 最近添加 / 文件夹」切换器，未新增第二排图标；窄屏仍走 compact 顶栏。
- S04 只挂载文件夹内容。继续学习 / 最近添加可见但不可点，避免空功能。
- 新装默认最近添加（初始化完成后再写 prefs）；未读完 library 不当空库。旧库首次默认文件夹。用户选过则恢复。
- 定位 / `returnToSearchResults` 强制文件夹真实目录，不恢复上次嵌套文件夹，不显示切换器。
- 缺失或回收目录回退最近有效祖先或根；会话内只推一层。
- overlay identity、compact 顶栏、搜索提示既有测试仍通过。

## 未跑

- 真机横竖屏与迷你播放卡压住切换器
- 整库 `flutter test`
- HomeScreen 全页泵送
- 既有 drop-targets 四文案失败未修
