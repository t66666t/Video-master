# Fluent Player 项目 零基础接手学习指南

欢迎！你拥有一款非常复杂且功能丰富的跨平台视频播放器（支持 Bilibili/YouTube 下载、视频合成、字幕解析等）。完全由 AI 生成出这样一个项目非常了不起。但要**脱离 AI、完全独立地手动维护它**，对于零基础的你来说，这将是一个极具挑战但也非常充实的旅程。

你有 **两个月** 的时间，我们需要极其专注、高效地分配这 60 天。

---

## 🎯 核心语言与技术栈解析

这个项目虽然涉及多个平台（Windows, macOS, Android 等），但你**不需要**学习所有的底层语言（如 C++, Kotlin, Swift）。
你只需要专注于核心：
1. **Dart** (核心编程语言，占比 90%+)
2. **Flutter** (UI 与应用框架)
3. **关键第三方库** (Provider, MediaKit, Dio 等)

---

## 📅 两个月（8周）冲刺学习计划

### 📍 第一阶段：编程基础与 Dart 语言（第 1 - 2 周）
**目标**：掌握编程的核心概念，学会写基本的 Dart 代码。

* **第 1 周：编程扫盲与基本语法**
  * **什么是编程？** 理解变量、数据类型（String, int, bool, List, Map）。
  * **控制流**：学习 `if/else` 条件判断，`for` 和 `while` 循环。
  * **函数**：如何定义函数、传递参数、返回值。
  * **面向对象编程 (OOP)**：类 (Class)、对象、继承 (extends)、接口 (implements)。这是理解项目中 `models/` 目录下代码的关键。

* **第 2 周：Dart 高级特性（极其重要）**
  * **空安全 (Null Safety)**：理解 `?` 和 `!` 的区别（如 `String? name`）。
  * **异步编程 (Async/Await & Future)**：**必学！** 你的项目里有大量的下载、视频处理和文件读取，全都是异步的。
  * **流 (Stream)**：理解数据流（用于下载进度条、播放器进度）。

* **推荐资源**：
  * [Dart 官方中文文档](https://dart.cn/guides/language/language-tour)
  * B站搜索 “Dart 零基础教程”

---

### 📍 第二阶段：Flutter 框架与 UI 构建（第 3 - 4 周）
**目标**：理解屏幕上的按钮、列表是如何画出来的。

* **第 3 周：Flutter 基础**
  * **万物皆 Widget**：理解 `StatelessWidget` 和 `StatefulWidget`。
  * **布局系统**：学会使用 `Container`, `Row`, `Column`, `Stack`, `ListView`。
  * **UI 构建**：尝试自己写一个简单的页面（比如模仿你的 `home_screen.dart`）。

* **第 4 周：状态管理与路由**
  * **状态管理 (Provider)**：你的项目深度依赖了 `provider` 库（看 `main.dart` 里的 `MultiProvider`）。必须彻底搞懂它如何把数据传递给 UI。
  * **路由 (Navigation)**：页面之间的跳转（如从主页跳到播放页）。

* **推荐资源**：
  * [Flutter 官方中文网](https://flutter.cn/)
  * B站搜索 “Flutter Provider 教程”

---

### 📍 第三阶段：掌握项目专属技术栈（第 5 周）
**目标**：学习项目中用到的特殊工具。

* **网络请求 (Dio)**：了解如何发送 HTTP 请求（Bilibili 下载服务依赖它）。
* **本地存储与文件 (Path Provider)**：了解如何读取、写入本地文件。
* **视频播放 (MediaKit)**：你的项目使用 `media_kit` 播放视频，去阅读它的官方文档，了解如何初始化播放器和控制播放。
* **命令行与进程 (FFmpeg & yt-dlp)**：了解应用是如何在后台调用 `yt-dlp.exe` 和 `ffmpeg.exe` 的。

---

### 📍 第四阶段：解剖你的项目（第 6 - 7 周）
**目标**：把学到的知识和你的代码结合起来，能看懂自己的项目。

你的项目结构（`lib/` 目录）解析：
1. `models/`：数据模型（如视频信息、字幕结构）。
2. `screens/` 或 `pages/`：界面（UI 页面）。
3. `widgets/`：可复用的 UI 小组件（如按钮、播放控制条）。
4. `services/`：核心业务逻辑（如下载、视频合成、配置保存）。
5. `utils/`：工具函数（如时间转换、链接解析）。

* **实战任务**：
  * **阅读代码**：从 `lib/main.dart` 开始，顺藤摸瓜，看懂它是如何启动的。
  * **添加小功能**：尝试修改主页的标题，或者修改某个按钮的颜色。
  * **Debug 调试**：学会使用断点 (Breakpoint)，看看代码运行时变量的值。

---

### 📍 第五阶段：维护、版本控制与独立（第 8 周）
**目标**：建立专业开发者的工作流。

* **Git 版本控制**：学习 `git commit`, `git push`, `git branch`。修改代码前先建分支，改坏了随时可以回退。
* **重构与 Bug 修复**：
  * 尝试人为制造一个错误，然后通过报错日志 (Console Log) 去定位它。
  * **关于原生代码的警告**：你的项目里有 `windows/`, `android/`, `ios/` 文件夹。作为初学者，**不要去碰它们**。只有当你需要接入极度底层的硬件 API 时才需要了解，目前 Dart 代码足以完成 95% 的工作。

---

## 💡 成功关键与学习建议

1. **不要焦虑**：这个项目非常庞大，即便是资深程序员接手也需要一周时间看代码。初期看不懂是 100% 正常的。
2. **多写少看**：不要只看视频，必须跟着敲代码。自己新建一个空白 Flutter 项目练手，然后再回来看这个大项目。
3. **善用日志**：学会写 `print('目前运行到了这里: $variable');`，这是排错的最好方法。
4. **遇事查文档**：脱离 AI 的第一步就是习惯去 [StackOverflow](https://stackoverflow.com/) 或者官方文档寻找答案。

祝你在这两个月里突飞猛进！如果有具体的代码疑问，可以随时针对某一个文件进行深入学习。