# Fluent_Player 项目介绍文档

> 本文档完全基于对项目源码的直接分析编写,未参考项目内任何既有文档,目的是为后续代码编写提供一份可靠的工程地图。
> 文档描述的是"代码事实",而不是产品承诺:所有结论均可通过阅读对应源码验证。

---

## 1. 项目概览

| 项 | 内容 |
|---|---|
| 应用名 | Fluent_Player(代码内 MaterialApp title 为 `Fluent_Player`;Windows 窗口标题 `Fluent Player`;macOS 产物 `fluent_player.app`) |
| Dart 包名 | `video_player_app` |
| 定位 | 跨平台本地媒体播放器,同时集成了 B 站/YouTube 下载、字幕全生命周期管理、弹幕、AI 转写、OCR 硬字幕识别、视频合成等能力 |
| 目标平台 | **Android、iOS、Windows、macOS**(用户要求兼容的四个平台);此外代码保留 Linux 支持、web 仅有模板(无实际功能) |
| 版本 | `1.0.0+2`(pubspec.yaml);Dart SDK `^3.8.1`,Flutter stable 渠道 |
| 代码规模 | `lib/` 下 151 个 Dart 文件、约 10 万行;`test/` 下 61 个测试文件 |

### 主要功能清单

1. **媒体库管理**:文件夹(合集)树状组织、网格/列表双视图、圈选/框选/拖拽排序、拖入文件导入(desktop_drop)、批量导入、压缩包结构化导入、回收站(软删除)、私有存储复制导入、缩略图磁盘+内存两级缓存。
2. **播放**:全平台统一 media_kit(libmpv)解码后端,支持视频/纯音频、断点续播、播放列表(文件夹级)、上一集/下一集、倍速(可锁定)、长按临时倍速、双击快进快退、画面比例/镜像/旋转、进度条悬停预览、下一个视频预加载。
3. **字幕**:SRT/VTT/ASS/LRC 文本字幕自研解析、PGS 位图字幕自研解析、内嵌字幕探测与提取、同目录外挂字幕自动发现与匹配、双语字幕、在线翻译(5 引擎)、样式系统(中英分字体)、屏幕位置拖动吸附、字幕边栏(列表/文章模式)、字幕编辑器、任务字幕存储。
4. **弹幕**:B 站弹幕 XML→ASS 转换、基于 ASS 的播放器弹幕渲染引擎(图集纹理缓存+性能自适应)。
5. **B 站下载**:BV/av/ep/ss 链接解析、WBI 签名、二维码/SESSDATA 登录、DASH 音视频分片下载与断点续传、ffmpeg 合并、弹幕下载、导入媒体库。
6. **YouTube 下载**:基于 yt-dlp(内置二进制/内嵌 Python),支持链接解析、元数据预览、画质选择、批量下载、cookies 导入、在线更新 yt-dlp。
7. **视频合成**:把字幕烧录(硬字幕/软字幕 mov_text)进视频,支持近似渲染(ASS 滤镜)与精确渲染(离屏 PNG 逐帧 overlay)两种模式。
8. **AI 转写**:调用 B 站必剪 ASR 接口做语音转文字,任务队列管理、断点续传、结果落库为托管字幕。
9. **OCR 字幕**:内置 PaddleOCR(PP-OCRv4)ONNX 模型,本地识别画面硬字幕,区域框选、抽帧去重、字幕样式学习、时间轴合并导出 SRT。
10. **系统集成**:通知栏/锁屏/耳机媒体控制(audio_service)、Android 前台媒体服务、后台音频(iOS)、亮屏协调(wakelock_plus)、音量/亮度手势、桌面快捷键体系、系统级"分享/打开方式"接入(Android intent、iOS document)。

---

## 2. 技术栈与关键依赖

### 2.1 核心播放方案(最重要的架构决策)

项目**弃用了 video_player 各平台的原生实现**,通过自研平台适配器把 `media_kit`(libmpv/FFmpeg)注册为 `video_player` 的 `VideoPlayerPlatform`:

- `lib/platform/windows_video_player_media_kit.dart`:`NativeVideoPlayerMediaKit.ensureInitialized()` 在 `main()` 早期调用,在 Android/iOS/macOS/Windows/Linux 全部注册 `_NativeMediaKitVideoPlayer extends VideoPlayerPlatform`。
- 效果:所有平台共用同一套宽编解码器(容器/编码不再受 OS 限制),且适配器显式关闭了 libass 内嵌字幕渲染(`PlayerConfiguration(osc: false, libass: false)`),因为字幕 UI 完全由应用自绘。
- 上层代码(`MediaPlaybackService` 及所有播放页)仍然使用标准的 `VideoPlayerController` API,不知道底层是 media_kit。**新增播放相关代码时不要绕过 `video_player` 接口直接调 media_kit**。

### 2.2 依赖分组(摘自 pubspec.yaml)

| 分组 | 依赖 |
|---|---|
| 播放 | `video_player`、`media_kit`、`media_kit_video`、`media_kit_libs_{android_video, ios_video, macos_video, windows_video, linux}`、`video_player_platform_interface` |
| 状态管理 | `provider` |
| 持久化 | `shared_preferences`、`path_provider` |
| 文件/媒体访问 | `file_picker`、`image_picker`、`photo_manager`、`wechat_assets_picker`、`open_filex`、`cross_file`、`desktop_drop`、`permission_handler` |
| 下载/网络 | `dio`、`dio_cookie_manager`、`cookie_jar`、`crypto`、`archive`、`url_launcher` |
| 媒体处理 | `ffmpeg_kit_flutter_new`、`fast_gbk`(GBK 解码) |
| 桌面集成 | `window_manager`、`screen_brightness`、`volume_controller` |
| 系统媒体 | `audio_service`、`audio_session`、`wakelock_plus`、`share_plus` |
| AI/OCR | `onnxruntime_v2` |
| UI/杂项 | `scrollable_positioned_list`、`linked_scroll_controller`、`video_thumbnail`、`uuid`、`intl`、`lpinyin`、`flutter_lyric`、`qr_flutter`、`image` |
| 开发 | `flutter_lints`、`glados`(属性测试)、`flutter_launcher_icons` |

### 2.3 内置二进制资产(assets/binaries)

| 平台 | 内容 |
|---|---|
| Windows | `yt-dlp.exe`、`ffmpeg.exe`、`ffprobe.exe`(运行时由 `YtDlpBinaryInstaller` 解包到应用支持目录 `yt_dlp/`) |
| macOS | `arm64/`、`x64/` 各含 `yt-dlp`、`ffmpeg`(无 ffprobe) |
| Android | 仅 `ffmpeg`(yt-dlp 走 Chaquopy 内嵌 Python,见 §6.6) |
| Linux | 无内置二进制,依赖系统安装的 yt-dlp/ffmpeg |

### 2.4 OCR 模型资产(assets/models/ocr)

PaddleOCR PP-OCRv4 移动端 ONNX 模型,随 App 打包(共约 63MB):

- 检测:`ch_PP-OCRv4_det_mobile.onnx`
- 识别:`ch/en/japan/korean_PP-OCRv4_rec_mobile.onnx`、`latin_PP-OCRv3_rec_mobile.onnx` + 各语言字典 `.txt`

---

## 3. 目标平台与差异

| 能力 | Android | iOS | macOS | Windows | Linux |
|---|---|---|---|---|---|
| 解码器 | media_kit(libmpv) | media_kit | media_kit | media_kit(libmpv) | media_kit |
| yt-dlp 下载 | ✅ Chaquopy 内嵌 Python(yt_dlp_bridge 模块) | ❌ 原生层明确返回 `YT_DLP_UNSUPPORTED` | ✅ 原生 `Process` | ✅ 原生进程(flutter_window.cpp 通道) | 依赖系统 PATH |
| ffmpeg | FFmpegKit 为主(yt-dlp 场景存在 ffmpeg CLI 时亦走 CLI) | FFmpegKit | 内置 CLI(assets/binaries)优先 | ffmpeg_kit 插件 DLL + 内置 ffmpeg.exe/ffprobe.exe | 系统 |
| 后台音频 | audio_service 前台服务 | `UIBackgroundModes=audio` | ❌ | ❌ | ❌ |
| 文件导入 | 系统分享 intent + ACTION_OPEN_DOCUMENT 选择器(MainActivity 深度解析) | 系统分享/document 复制到 cache | 文件选择器 | 文件选择器 + 拖拽(desktop_drop) | — |
| 弹幕 | ✅ | ✅ | ✅ | ✅ | ✅ |
| OCR 加速后端 | NNAPI | CoreML | CoreML | DirectML(自定义 onnxruntime.dll 替换) | CPU |
| 缩略图生成 | `video_thumbnail` 插件 | `video_thumbnail` 插件 | 插件失败回退内置 ffmpeg CLI 抽帧 | 插件失败回退内置 ffmpeg CLI 抽帧 | 插件失败回退系统 ffmpeg 抽帧 |
| 大文件数据根目录 | — | — | — | ✅ 可在设置中迁移到任意盘 | — |

其他要点:

- **UI 分流**:桌面(Windows/macOS/Linux)直接进入横屏 `VideoPlayerScreen`;移动端(Android/iOS)进入 `PortraitVideoScreen`,横屏切换时通过零时长 `PageRouteBuilder` 传递 controller 所有权(见 §6.3)。
- **窗口管理**:桌面端 `window_manager` 初始化 1280×720 居中窗口,`SettingsService.toggleFullScreen()` 管理全屏,F11/Esc 为全局快捷键。
- **字体**:iOS 使用系统字体(`CupertinoSystemText`),其余平台默认 `MiSans`;`assets/fonts/` 内置 46 个字体文件(Inter、Roboto Condensed、Noto Sans/Serif CJK SC、方正黑体/楷体、OPPO Sans、Swei Gothic、Comic Relief 等),用于字幕样式选择。

---

## 4. 总体架构

### 4.1 分层结构

```
screens / widgets          —— UI 层(全部中文注释,StatefulWidget + setState 为主)
        │  Provider.of<T>(context) 读取;调用方法改变状态
        ▼
services                   —— 业务层(ChangeNotifier;核心服务多为单例工厂模式)
        │  依赖注入:构造后 initialize(...) 注入其他服务
        ▼
models + utils + platform  —— 数据模型(JSON 序列化)/ 纯函数工具 / 平台适配
        │
        ▼
外部世界:
  - video_player(→ media_kit 适配器)→ libmpv
  - ffmpeg / ffprobe / yt-dlp 进程(桌面)
  - FFmpegKit / FFprobeKit(移动端)
  - MethodChannel/EventChannel(Android MainActivity、iOS/macOS AppDelegate、Windows flutter_window)
  - onnxruntime(OCR)、audio_service(媒体会话)、shared_preferences/文件系统(持久化)
```

### 4.2 启动流程(main.dart)

1. `_configureImageCaches()`:限制图片内存缓存(桌面 600 项/256MB,移动 350 项/128MB),防止 Windows OOM。
2. `NativeVideoPlayerMediaKit.ensureInitialized()`:注册 media_kit 平台实现,失败可恢复。
3. `SettingsService().init()`(2 秒超时,失败不阻塞启动)。
4. 桌面平台初始化 `windowManager` 并显示最大化窗口。
5. 创建并注册全局服务:`LibraryService`(init 不 await)、`BatchImportService`、`TranscriptionManager`、`EmbeddedSubtitleService`、`BilibiliDownloadService`、`YtDlpDownloadService`、`VideoComposeManager`、`OcrSubtitleManager`、`PlaylistManager`、`ProgressTracker`、`MediaPlaybackService`、`SystemMediaSessionService`。
6. `LibraryService` 初始化完成后异步恢复上次播放状态(`_restorePlaybackState`:恢复播放列表与进度,但强制 `autoPlay: false`)。
7. 安装崩溃日志(`crash_log.txt`,1MB 上限)、`FlutterError.onError` + `runZonedGuarded` 双通道捕获。
8. `MultiProvider` 挂载全部服务,`MyApp` 提供主题(深色 Material 3)、全局快捷键(F11 全屏/Esc 退出全屏/Ctrl+R 重启 App)、`AppToast.navigatorKey`。

### 4.3 状态管理与依赖注入约定

- **Provider + ChangeNotifier** 是唯一状态管理方案;所有跨页面共享状态的服务都挂在 `MultiProvider` 上。
- **单例工厂模式**:`SettingsService`、`LibraryService`、`MediaPlaybackService`、`SystemMediaSessionService`、`PlaybackNavigationService`、`BatchImportService`、`SubtitleTranslationService` 等都是 `static final _instance = X._internal(); factory X() => _instance;`。**UI 中可以安全地 `Provider.of<T>(context, listen: false)` 拿到同一个实例**。
- **静态工具类**:`AppWakelockCoordinator`、`NativeVideoPlayerMediaKit`、`FFmpegUtils` 等无状态/全局状态用 static 方法。
- **初始化模式**:需要注入依赖的服务提供 `initialize({required ...})`,由 `main()` 显式注入;`init()` 方法自身允许失败并被调用方 `catchError`。
- UI 重建策略:进度条/歌词用 `ValueListenableBuilder`;`MediaPlaybackService.positionNotifier`(每帧)与 `coarsePositionNotifier`(低频)是全局时间轴;`RepaintBoundary` 隔离重绘;`Selector`/`Consumer` 做精准订阅。

---

## 5. 目录结构

```
lib/
├── main.dart                     # 入口:服务编排、主题、全局快捷键、崩溃日志
├── features/youtube_download/    # YouTube 下载(独立的 feature 目录结构)
│   ├── models/                   # youtube_download_models.dart(全部任务模型)
│   ├── platform/                 # yt_dlp_native_bridge.dart(MethodChannel/EventChannel)
│   ├── services/                 # installer/updater/download_service/request_builder 等 6 个
│   └── presentation/pages/       # yt_dlp_download_screen.dart
├── models/                       # 20 个数据模型(video_item、video_collection、subtitle_*、danmaku_*、bilibili_*、video_compose_models、ocr_subtitle_models 等)
├── platform/                     # windows_video_player_media_kit.dart(media_kit 适配器)
├── screens/                      # 13 个页面(home、video_player、portrait_video、music_player、bilibili_download、batch_*、recycle_bin、collection、subtitle_preview 等)
├── services/                     # 49 个服务
│   ├── bilibili/                 # B 站下载子服务 8 个(api/download/state_manager/download_manager/integrity/post_process/wbi_signer/grpc_models)
│   └── video_compose/            # 视频合成子服务 13 个(executor/orchestrator/precise_renderer/ass_renderer/预览/任务存储/清理等)
├── utils/                        # 20 个工具(parser、字幕工具、ffmpeg_utils、快捷键、toast、队列等)
└── widgets/                      # 37 个组件(播放控制、字幕、弹幕、OCR、合成面板、媒体库卡片等)

android/ ios/ macos/ windows/ linux/ web/   # 平台原生工程
assets/
├── binaries/                     # ffmpeg/ffprobe/yt-dlp 内置二进制
├── fonts/                        # 46 个字体
├── icon/                         # fluent-player.png
└── models/ocr/                   # PaddleOCR ONNX 模型
test/                             # 61 个测试文件(命名与被测文件对应)
.github/workflows/                # iOS 未签名 IPA 构建、macOS 构建
tool/ packages/                   # 空目录(无内容)
```

**重要约定**:`build/` 下全是生成产物,永不修改;`windows_backup/`、`apk_analysis/`、根目录散落的 `analyze_*.txt`、`dummy*.mkv`、`temp_*`、`ffmpeg2.zip` 等是历史遗留/临时文件,与当前源码无关。

---

## 6. 核心子系统详解

### 6.1 媒体库与主页

**核心文件**:`services/library_service.dart`(约 5600 行,全项目最大单文件)、`screens/home_screen.dart`(约 3700 行)、`services/settings_service.dart`、`services/batch_import_service.dart`、`utils/media_folder_scanner.dart`、`services/thumbnail_cache_service.dart`、`services/thumbnail_preload_manager.dart`、`screens/collection_screen.dart`、`screens/recycle_bin_screen.dart`、`screens/batch_import_screen.dart`、`services/temporary_storage_cleanup_service.dart`。

**LibraryService**(单例 ChangeNotifier):

- 内存结构:`_collections`(Map)、`_videos`(Map)、`_rootChildrenIds`(根级有序列表)。合集是树:`VideoCollection.childrenIds` 可同时含视频与子合集。
- 持久化:单一 `library.json`(schemaVersion=2),写入采用 `.tmp` + `.bak` 原子替换;保存有 20 秒防抖 + `SerialTaskQueue` 串行化;Windows 下数据根目录默认在可执行文件旁 `VideoPlayerData/`(可经 `SettingsService.largeDataRootPath` 迁移到其他盘,带 `.bak`/`.tmp` 全量搬迁),其余平台为应用文档目录。
- 支持扩展名:视频 `.mp4/.mov/.avi/.mkv/.flv/.webm/.wmv/.3gp/.m4v/.ts/.rmvb/.mpg/.mpeg/.f4v/.m2ts/.mts/.vob/.ogv/.divx`,音频 `.mp3/.m4a/.wav/.flac/.ogg/.aac/.wma/.opus/.m4b/.aiff`。
- 导入模式:`copyImportedMediaToPrivateStorage`(私有复制,opt-in)/ 保持原路径 / 压缩包移动;文件夹扫描与压缩包解包在后台 isolate(`compute`)执行以防 Windows 闪退;压缩包导入带写入预算与路径穿越防护,用 `.import_in_progress.json` 标记防中断损坏,支持 zip/tar/tar.gz/tar.bz2/tar.xz。
- 兼容转码:`ensureCompatiblePlaybackFile()` 对播放器不支持的封装用 ffmpeg 转码为兼容副本(`compatible_audio/`,文件名 = sha256(path|size|mtime)),纯音频也复用此机制(`AudioPlaybackCompatibilityService`)。
- 缩略图:`video_thumbnail` 插件失败时回退 ffmpeg 抽帧;`ThumbnailCacheService` 内存 LRU + 磁盘文件;`ThumbnailPreloadManager` 按可视区预加载。
- 回收站:软删除(`isRecycled` + `recycleTime`),可恢复或彻底删除;删除时级联清理缩略图(磁盘文件 `thumbnails/{videoId}.jpg`)、弹幕边车、任务字幕等派生文件;彻底删除仅限应用管理的目录(内部私有/临时/Android 外部/B站下载目录),不会误删外部原文件。

**HomeScreen**:

- 单页"我的媒体库",**无底部 Tab 栏**;含根级合集+视频混排(网格/列表);连续点击标题 5 次切换隐藏的"导出设置"按钮显隐。
- 选择交互:长按进入选择模式、拖圈选(移动端)、框选(桌面 CustomPainter)、拖拽排序/拖入文件夹、Esc/PopScope 退出。
- FAB(`widgets/video_action_buttons.dart`)入口:批量字幕、新建合集、批量导入、B 站下载、YT-DLP 下载,以及隐藏的"临时文件清理"(TemporaryStorageCleanupService 扫描报告)。
- 剪贴板监听:B 站链接弹窗引导跳转下载页;Android 分享 intent 通道(`com.example.video_player_app/share_intent`)接收外部文件导入。

**SettingsService**(单例 ChangeNotifier):

- 基于 `_RegisteredSetting<T>` 注册表(120+ 个 key),统一提供:默认值、读取、写入、`normalize`(clamp/枚举校验)、`apply`(同步改内存)。
- **更新顺序约定:先同步 apply 内存,再 `notifyListeners()`,最后异步落盘**——保证运行中的任务(如批量字幕)立即看到新值;互斥设置(如软字幕两种嵌入模式)用 `_updateRegisteredSettingsAtomically` 原子更新。
- 样式类设置(字幕文字/布局/幽灵模式/音频字幕样式、弹幕样式)以 JSON 字符串存 shared_preferences,含新旧格式迁移逻辑(如 `subtitleStyle` → `subtitleTextStyle` + `subtitleLayoutLandscape/Portrait`)。

### 6.2 播放核心

**核心文件**:`services/media_playback_service.dart`(约 3100 行)、`services/playlist_manager.dart`、`services/progress_tracker.dart`、`services/playback_timeline_clock.dart`、`services/system_media_session_service.dart`(约 1150 行)、`services/app_wakelock_coordinator.dart`、`services/playback_navigation_service.dart`、`services/audio_playback_compatibility_service.dart`、`platform/windows_video_player_media_kit.dart`、`utils/media_duration_probe.dart`。

**MediaPlaybackService**(单例 ChangeNotifier,全局唯一播放状态机):

- 状态枚举 `PlaybackState { idle, loading, playing, paused, error }`;对外 API:`play/pause/resume/stop/seekTo/setVolume/toggleMute/setPlaybackSpeed/beginTemporaryPlaybackSpeed/endTemporaryPlaybackSpeed/playNext/playPrevious/playPlaylistItem/setController/clearController` 等。
- 字幕状态:`setSubtitles/setSubtitleState/loadSubtitlePathsForCurrentItem/clearSubtitleState`、`subtitleRevision` 供 UI 增量同步;内嵌 `SubtitleTimelineResolver` 二分定位当前字幕。
- **时间轴**:`positionNotifier`(每帧,驱动进度条和弹幕)与 `coarsePositionNotifier`(低频,供轻量进度组件);`PlaybackTimelineClock` 做 VSync 帧插值,原生采样只做 ≤0.5% 斜率漂移校正,大偏差视为 seek 不校正。
- **竞态防护(全项目最重要的模式)**:`_playRequestId/_seekRequestId/_subtitleLoadRequestId/_playbackSpeedRequestId/_preloadedRequestId` 代际计数,异步回调回来先校验 id 是否过期;倍速命令走 `_playbackSpeedCommandTail` 尾链串行;seek 后 180/650ms 两段校验(容差 450ms)确认落点;暂停/切集/seek 后 500ms 落盘进度。
- 预加载:进度 ≥80% 时后台初始化下一集 controller,切换时热替换;旧 controller 后台 dispose(上限 3 个防泄漏)。
- 播放完成将进度重置为 0(避免"已完成视频无法重播");末尾 500ms 内恢复进度也归零。

**PlaylistManager**:播放列表(文件夹级),`loadFolderPlaylist(folderId, currentItemId)`、`reloadPlaylist`、`getNext/getPrevious`。

**ProgressTracker**:进度持久化——实际写入 `VideoItem.lastPositionMs`(经 LibraryService);另有 `playback_state_snapshot`(shared_preferences)保存"上次播放的条目/位置/播放列表文件夹",供启动恢复。

**SystemMediaSessionService**(单例):audio_service/audio_session 集成,通知栏/锁屏/耳机控制;800ms 节流发布快照、`_publishRevision` 防陈旧发布;`_SystemMediaAudioHandler extends BaseAudioHandler with QueueHandler, SeekHandler` 把远程指令路由回播放服务。仅 Android/iOS 启用。

**AppWakelockCoordinator**:多理由亮屏协调(static `Set<String> _activeReasons`),理由有 `mediaPlayback`、`bilibili_download_processing`、`yt_dlp_download_processing`。

### 6.3 播放器 UI

**核心文件**:`screens/video_player_screen.dart`(约 5800 行)、`screens/portrait_video_screen.dart`(约 4700 行)、`screens/simple_video_player_screen.dart`、`screens/simple_video_preview_screen.dart`、`screens/music_player_screen.dart`、`widgets/video_controls_overlay.dart`、`widgets/music_playback_controls.dart`、`widgets/mini_playback_card.dart`、`widgets/playlist_bottom_sheet.dart`、`widgets/episode_picker_panel.dart`、`widgets/playback_card_layout.dart`、`widgets/music_lyric_view.dart`、`widgets/music_album_cover.dart`、`widgets/music_cd_cover.dart`。

- **入口分流**:`home_screen._buildVideoPlayerRoute()` —— 桌面 `PageRouteBuilder`(零时长、opaque)→ `VideoPlayerScreen`;移动 `MaterialPageRoute`(带 `PlaybackNavigationService.portraitRouteSettings`)→ `PortraitVideoScreen`。
- **横竖屏切换**:竖屏页 `_goToLandscape()` 把 `_controller` + `videoItem` + `skipAutoPauseOnExit: true` 传给横屏页,设置 `_isControllerOwner=false` 移交所有权;横屏页 `initState` 强制 `landscapeLeft/Right` + immersive;返回竖屏按需 `_initPlayer()` 重同步。
- **轻量预览页**:`SimpleVideoPlayerScreen` 自建独立 controller(进前暂停主播放、退出恢复),用于视频合成预览;`SimpleVideoPreviewScreen` 复用 `MediaPlaybackService.play()`,用于批量导入预览。
- **音乐播放器**:Apple Music 风格全屏页(实验性,播放页五连击标题进入);`Selector<MediaPlaybackService, _PlayerSnapshot>`(快照不含 position)+ `positionNotifier` 驱动;歌词用 `MusicLyricView`(基于 flutter_lyric,支持滚动定位、字号缩放)、`MusicCdCover` 自绘旋转 CD。
- **手势(集中在 VideoControlsOverlayState 的 RawGestureDetector)**:双击(自实现 300ms 判定)——中部播放/暂停、左右 20% 快退/快进(可按字幕句跳转);长按 350ms 临时倍速;水平拖动滑动进度(顶部 25% 取消区);垂直拖动左亮度右音量;桌面滚轮调音量、MouseRegion 悬停显示控制栏(3 秒自动隐藏,锁定时不隐藏,隐藏时 `SystemMouseCursors.none`)。
- **功能入口**:右侧工具栏(设置/字幕库/字幕编辑/合成视频/OCR 字幕/字幕样式/移动字幕/弹幕设置…)、底部(选集/上下集/倍速/字幕开关/静音)、左侧锁屏(移动端);AI 转写以侧边栏形式打开;桌面有完整快捷键体系(§6.9)。
- **状态订阅**:页面 `addListener(_onPlaybackServiceChange)` + `service.subtitleRevision` 增量同步;`_subtitleRefreshToken/_postInitWorkToken` 令牌防过期回调。

### 6.4 字幕系统

**核心文件**:服务 `embedded_subtitle_service.dart`、`subtitle_discovery_service.dart`、`subtitle_translation_service.dart`、`subtitle_timeline_resolver.dart`、`task_subtitle_storage_service.dart`;工具 `subtitle_parser.dart`、`pgs_parser.dart`、`subtitle_converter.dart`、`subtitle_file_matcher.dart`、`subtitle_display_resolver.dart`、`subtitle_drag_snap.dart`、`subtitle_util.dart`、`subtitle_file_picker.dart`;模型 `subtitle_model.dart`(`SubtitleItem`,位图字幕带 `imageLoader` 懒加载)、`subtitle_style.dart`、`managed_subtitle_asset.dart`、`subtitle_display_state.dart` 等;UI `subtitle_overlay.dart`、`subtitle_display_layer.dart`、`subtitle_sidebar.dart`、`subtitle_editor_panel.dart`、`landscape_subtitle_editor_sidebar.dart`、`subtitle_position_sidebar.dart`、`subtitle_settings_sheet.dart`、`subtitle_management_sheet.dart`、`subtitle_preview_screen.dart`。

- **解析(全自研)**:SRT/VTT/ASS/LRC 文本(编码探测:BOM→UTF16→UTF8→GBK fast_gbk);PGS(.sup)位图(RLE 解码、YCbCrA→ARGB、生成 BMP,`compute` 后台渲染);VobSub 先经 ffmpeg 转 `.sup` 再统一解析。
- **内嵌字幕**:非 Windows 用 FFprobeKit,Windows 优先 media_kit Player 流、回退 ffmpeg 命令+正则;提取用 `-map 0:s:N`,codec 决定扩展名(hdmv_pgs_subtitle/dvd_subtitle→.sup,ass→.ass,默认 .srt);提取有 size+mtime 缓存;用同步 `execute` 避免 `executeAsync` 竞态(源码注释明确)。
- **双语**:主/副轨道经 `matchSubtitleTracks()`(重叠评分+索引对齐)配对;边栏按 `lineFilterMode` 显示双行合并/主/副。
- **翻译**:纯在线,5 引擎(`SubtitleTranslateProvider { google, bing, so360, mymemory, reverso }`),单例 ChangeNotifier 持有任务状态(UI 销毁重开可恢复);无内容缓存,输出文件存在则加时间戳改名;Reverso 仅桌面(经系统 curl 绕过 TLS 指纹拦截)。
- **样式**:`SubtitleTextStyle`(中/英独立字体与字重、颜色、背景、描边、阴影、斜体/下划线)+ `SubtitleLayoutStyle`(字号/副字号/行距/字距);屏幕位置 `Alignment(x,y)` 持久化(JSON+旧 key 双写);按 720 参考高度缩放;中英混排按 CJK 码点切 TextSpan。
- **编辑器**:文本实时编辑(220ms 防抖持久化)、undo/redo 快照栈(80/500 上限)、删除模式、分组;时间轴无拖动调时(点击行 seek、键盘导航、自动跟随)。
- **托管字幕资产**:`TaskSubtitleStorageService` 目录 `dataRoot/subtitles/tasks/<videoId>/`(videoId 仅允许 `[A-Za-z0-9_-]` 防穿越);`ManagedSubtitleAsset` 记录 kind(ai|translated|imported|manual|embedded|downloaded|ocr|cached)与 `sourceAssetId` 生成链,删除时级联清理派生资产。

### 6.5 B 站下载与弹幕

**核心文件**:`services/bilibili/bilibili_api_service.dart`、`bilibili_download_service.dart`(约 3300 行)、`bilibili_download_state_manager.dart`、`download_manager.dart`、`download_integrity.dart`、`post_process_task_queue.dart`、`wbi_signer.dart`、`grpc_models.dart`(死代码,见 §10);模型 `bilibili_models.dart`、`bilibili_download_task.dart`、`danmaku_model.dart`、`danmaku_style.dart`;工具 `bilibili_url_parser.dart`、`bilibili_danmaku_ass.dart`、`danmaku_ass_parser.dart`;UI `bilibili_download_screen.dart`、`bilibili_download_list_projection.dart`、`bilibili_login_dialogs.dart`、`danmaku_overlay.dart`、`danmaku_settings_dialog.dart`。

- **全自研实现,不调用外部 BBDown 程序**(仅命名上借用了 BBDown)。
- **解析**:`BilibiliUrlParser` 正则提取 BV/av/ep/ss;短链 b23.tv/bili2233.cn 先 HEAD 302;番剧 `pgc/view/web/season`,视频 `x/web-interface/view`(有 ugc_season 按合集处理)。
- **API 与签名**:取流走 `x/player/wbi/playurl`(qn=0、fnval=4048 DASH、fourk=1;无 cookie 加 try_look=1),字幕/备用通道走 `x/player/wbi/v2`,参数经 `WbiSigner.sign`(从 nav 接口取 wbi_img 的 img/sub key 混排 + md5)。
- **登录**:`PersistCookieJar` 存 SESSDATA(`appDocDir/.bilibili_cookies`),二维码 generate/poll 或手动输入。
- **下载与合并**:DASH 音视频分片下载、`Range` 头断点续传(`DownloadPartResumeState`,content-range 校验不匹配则重下);合并:非 Windows 用 FFmpegKit、Windows 用 `Process.start`(copy 编码、HEVC 加 `-tag:v hvc1`、`-movflags +faststart`、字幕 mov_text),时长校验(±10%+5s),不兼容时 `_repairVideo` 转码 libx264/aac 原子替换;合并/修复各一条 `SerialPostProcessQueue` 带看门狗超时(6/45min、8/50min)。
- **弹幕**:`fetchDanmakuXml`(comment.bilibili.com/$cid.xml,gzip/deflate 解压)→ `xmlToAss`(泳道分配)→ 写 `*.danmaku.ass` 边车。
- **任务模型**:`DownloadStatus { pending, queued, fetchingInfo, downloading, merging, checking, repairing, completed, failed }`;持久化 `bilibili_download_tasks.json`(脏快照防抖批量写、`compute` 后台编解码);重启时活跃任务修正为可重试失败。进度通知 280ms 起步节流(Windows 按负载上调至 420–950ms)+ `episodeRevision` 精准刷新;续传态 900ms 防抖落盘;Windows 下载并发上限 2。注意:`downloadSpeed/downloadSize` 字段被复用于承载"已合成/正在导出"等文本提示(隐性约定)。
- **弹幕渲染**:`DanmakuOverlay` 消费 `ValueListenable<Duration> position`,CustomPainter 重绘;ASS 后台 `compute` 解析;`_DanmakuActiveSet` 二分活跃区间;文本图集纹理(drawRawAtlas,2048×1024 页,LRU,移动 64MB/桌面 128MB 预算),失败降级 TextPainter;`_DanmakuPerformanceGovernor` 按帧耗时自适应降级。无关键词/颜色过滤(仅去重+限流)。
- **安全**:临时文件名 key 用 `[^A-Za-z0-9_-]` 清洗防路径穿越;删除仅限临时目录内(`_isWithinTempDir`)。

### 6.6 YouTube 下载(yt-dlp)

**核心文件**:`features/youtube_download/` 全部;主服务 `yt_dlp_download_service.dart`(约 5100 行)、`platform/yt_dlp_native_bridge.dart`、`services/yt_dlp_binary_installer.dart`、`yt_dlp_binary_updater.dart`、`yt_dlp_input_url_extractor.dart`、`yt_dlp_meta_parser.dart`、`yt_dlp_request_builder.dart`、`yt_dlp_video_format_selector.dart`、`models/youtube_download_models.dart`、`presentation/pages/yt_dlp_download_screen.dart`(约 3700 行)。

- **二进制获取**:桌面从 assets 解包到 `<ApplicationSupport>/yt_dlp/`(Windows 还有 ffmpeg/ffprobe);macOS 按 `uname -m` 选 arm64/x64;`.version`/`.source` 标记区分 bundled 与 official-release;在线更新仅 Windows/macOS(GitHub releases/latest + SHA2-256SUMS 校验、`.download→rename` + `.bak` 回滚)。Android 无独立二进制,原生层 Chaquopy 内嵌 Python 跑 `yt_dlp_bridge` 模块;iOS 原生层明确不支持(返回 `YT_DLP_UNSUPPORTED`)。
- **URL 解析**:多行输入/整段分享文本正则提取(无 scheme 自动补 https、去中英文标点),短链交给 yt-dlp。
- **元数据**:`resolveUrl` → `resolveYoutubeMeta`(95s 超时)→ 后台 isolate `YtDlpMetaParser.parse`;失败用"更安全配置"(清空 player_client/visitor_data/po_token)回退重试。
- **格式选择**:`YtDlpVideoFormatSelector` 按质量偏好选最接近分辨率,优先 mp4+h264;音视频分离时 `视频id+音频id` 合并;桌面依赖 ffmpeg 转封装(默认 mkv)。
- **执行**:不走 Dart Process(除缩略图/转码),`YtDlpRequestBuilder` 生成参数(`--newline --progress --force-overwrites --restrict-filenames --embed-metadata --write-thumbnail` + `--print before_dl/after_move` 标记),经 MethodChannel 交原生层跑 yt-dlp 进程(Windows 在 flutter_window.cpp、macOS 在 AppDelegate、Android 在 MainActivity 的 Python);原生层解析进度,经 EventChannel 回传 `task_queued/started/step/progress/post_processing/completed/failed/cancelled/retrying`;Dart 端按任务数/页面活性节流(250ms~1200ms)。Android 本地后处理走 FFmpegKit,中间产物带 `ytdlp_` 前缀、24 小时自动清理。
- **错误恢复**:`_mapFailureText` 归类失败类型;可恢复失败自动回退(上限 10 次,逐步应用降并发/加超时/限速/换 player_client/启用 cookies/UA/visitor_data/po_token/代理,1~6s 退避)。
- **持久化**:shared_preferences keys `yt_dlp_tasks_v1/_completed_tasks_v1/_failed_tasks_v1/_session_config_v1/_download_preferences_v1` 等,JSON 后台 `compute()` 编解码,进度 1s 防抖;启动时进行中任务重置为 `paused`。
- **依赖**:LibraryService 导入(`addSingleVideo`、`MediaSourceRef`)、TaskSubtitleStorageService、AppWakelockCoordinator、TemporaryStorageCleanup。

### 6.7 视频合成

**核心文件**:`services/video_compose_manager.dart`(门面)+ `services/video_compose/` 13 个子服务、`models/video_compose_models.dart`、`widgets/video_compose_panel.dart`、`services/ffmpeg_service.dart`(死代码,见 §10)、`utils/ffmpeg_utils.dart`。

- **功能定位**:把主/副字幕烧录进视频——近似模式生成 `.ass` 交给 ffmpeg `ass` 滤镜(硬字幕),或内嵌为可切换软字幕(mov_text);**不含裁剪、不处理弹幕**。
- **状态机**:`VideoComposeStage { queued → preparing → rendering → finalizing → completed/failed }`,由 `VideoComposeManager._processQueue`(ChangeNotifier + 串行队列)驱动,总进度映射 preparing 0~0.2、rendering 0.2~0.92、finalizing 0.96、completed 1.0;分辨率枚举 `VideoComposeResolution(source/p360…p2160)`、渲染模式 `VideoComposeRenderMode(precise/approximate)`;任务模型为 `VideoComposeTaskState`;持久化 `compose_tasks.json`(应用文档目录,仅在阶段变化时保存;JSON 数组格式,`createdAt/completedAt` 存毫秒时间戳、`stage/resolution` 存枚举 index、`renderMode` 存 name,逐条容错读取),**枚举以 `index` 持久化(枚举重排会错位,是已知坑)**,重启时未完成任务置为 failed。`VideoComposeRequest` 承载全部入参:videoId/videoPath/主副字幕路径/renderSecondarySubtitle/continuousSubtitle/embedSoftSubtitles/softSubtitleOnly/softSubtitleTracks/resolution/renderMode/横竖屏 subtitleStyle 与 subtitleAlignment/splitSubtitleByLine/subtitleItemGap/outputPath 等。
- **渲染管线(三条,互斥分工)**:
  1. `VideoComposePreviewController`:界面实时预览,复用 `SubtitleOverlay` widget,不落盘;
  2. `VideoComposePreciseRenderer`:精确模式,按字幕时间边界分场景、逐场景 Flutter 离屏渲染 PNG(超采样 DPR 上限 8192,8K 源可能降采样;逐帧 `precacheImage`)+ `ffconcat` + overlay 滤镜,支持 PGS 图片字幕(`imageLoader`);
  3. `VideoComposeAssRenderer`:近似模式,生成 .ass 交 ffmpeg。
- **编排**:`VideoComposeOrchestrator` 串联探测(`VideoComposeProbeService`:桌面 ffprobe 进程 / 移动 FFprobeKit,处理旋转 tag 与 SAR、90/270° 时交换宽高、输出尺寸取偶数)→字幕加载(`VideoComposeSubtitleService`:按扩展名分流 PgsParser / SubtitleConverter / SubtitleParser)→渲染→执行(`VideoComposeExecutor`)→校验;字体收集(`VideoComposeFontService`:桌面直接用打包的 `flutter_assets/assets/fonts`,移动端从 rootBundle 拷贝到临时 `compose_fonts` 并 `FFmpegKitConfig.setFontDirectory`,内置字体缺失会报错)与输出路径(`VideoComposeOutputPathService`:默认目录 Win/macOS→可执行文件目录、Android→Download/ComposedVideos(需 manageExternalStorage 权限)、其余→文档目录;文件名清洗 `\/:*?"<>|` 并以时间戳防冲突)为支撑服务;`VideoComposeArtifactCleaner` 跟踪并清理中间产物(删除失败改重命名重试,输出文件 ≤1024 字节判异常)。
- **FFmpeg 双路径**:桌面(Win/macOS)`Process.start` + 内置 `ffmpeg.exe/ffprobe.exe`(经 `FFmpegUtils` 解析,yt-dlp 安装器兜底);移动端 `ffmpeg_kit_flutter_new`(`FFmpegKit.executeAsync`/`FFprobeKit`)。输出参数典型为 `-c:v libx264 -preset veryfast -crf 20 -pix_fmt yuv420p -c:a copy`;精确模式 filter_complex:`[0:v]<scale/pad/ass>[base]` + `[1:v]format=rgba,setpts=PTS-STARTPTS[sub]` + `overlay=0:0:eof_action=pass:repeatlast=1`;近似模式直接 `-vf scale/pad,ass=filename:fontsdir`;路径转义:`\`→`/`、`:`→`\:`、`'`→`\'`。
- **复用关系**:`ffmpeg_utils.dart` 被 8 处 import(合成、OCR 字幕、内嵌字幕、转录、视频预览、B站下载、时长探测)——**改 FFmpegUtils 影响面大**;`ffmpeg_service.dart` 无任何引用。

### 6.8 OCR 与 AI 转写

**核心文件**:`services/ocr_model_manager.dart`、`ocr_inference_engine.dart`、`ocr_processing_worker.dart`、`ocr_subtitle_manager.dart`(约 1200 行)、`transcription_manager.dart`(约 2400 行)、`bcut_asr_service.dart`;模型 `ocr_subtitle_models.dart`、`transcription_status.dart`、`batch_subtitle_task_view.dart`;UI `ocr_region_editor.dart`、`ocr_region_preview.dart`、`ocr_subtitle_panel.dart`、`ai_transcription_panel.dart`、`task_queue_table.dart`、`screens/batch_subtitle_screen.dart`。

- **OCR 引擎**:onnxruntime_v2;模型全部内置(`rootBundle.load`),首次使用物化到 large-data 目录 `ocr_models/ppocr_v4_mobile/`(逐文件 SHA256 校验)——"下载/安装"字样实为本地复制;`OrtEnv` → `OrtSession.fromBuffer`;平台后端 Windows→DirectML、macOS/iOS→CoreML、Android→NNAPI,加速失败回退 CPU。
- **Isolate**:`Isolate.spawn(_ocrWorkerMain)` + `@pragma('vm:entry-point')`,SendPort/ReceivePort + 请求 id + Completer;CPU 与加速两引擎同帧测速选快者。
- **OCR 流程**:抽帧(移动 video_thumbnail、桌面 ffmpeg;批量用 ffmpeg filter 链 crop→scale≤640→fps=10→mpdecimate 去重→showinfo 记录 PTS)→区域框选(`NormalizedOcrRegion`,归一化坐标直通 ffmpeg crop)→识别判定(灰度签名 SSIM 变化检测 + 双帧 Levenshtein≥0.78 防闪烁,`OcrSubtitleStyleFilter` 学习中位行高/中心过滤水印)→合并时间轴(编辑距离≥0.82 合并、剔除常驻水印行)→导出 SRT(`TaskSubtitleStorageService` 分配路径,`registerManagedLocalSubtitle(kind: ocr)` 落库)。
- **AI 转写**:`TranscriptionManager`(ChangeNotifier)组合 `BcutAsrService`,调用 **B 站必剪未鉴权接口** `member.bilibili.com/x/bcut/rubick-interface`(model_id=8):申请上传授权→分块 PUT(ETag)→提交→创建任务→轮询(120×2s);状态机 `TranscriptionStatus` 八态(idle/extracting/uploading/transcribing/embedding/completed/error);每任务 30 分钟超时,`_JobCancellation` 贯通 Dio CancelToken 与 ffmpeg;持久化 `transcription_queue_cache.json`(500ms 防抖),重启后 active 任务转为可重试失败。
- **批量字幕**:`BatchSubtitleScreen` + `TaskQueueTable`(ReorderableListView,仅 idle 可拖);mediaKey = `id:xxx` 或 `path:xxx`(Windows 小写);支持内部媒体选择、外部文件/文件夹导入、全部开始、软字幕内嵌(mov_text,含复制+内嵌/删除原件/前后缀等互斥选项)。

### 6.9 通用工具与桌面快捷键

- **AppToast**(`utils/app_toast.dart`):全局 OverlayEntry Toast,`show/showLoading/showProgress/updateProgress/dismiss`,带 action 按钮与进度条;`AppToastHandle` 用 presentationId 使过期 dismiss 自动失效;必须挂 `AppToast.navigatorKey`(main.dart 已挂)。
- **SerialTaskQueue**(`utils/serial_task_queue.dart`):严格按提交顺序串行执行异步任务,单任务失败不阻塞队列——用于 library.json 保存、下载后处理等。
- **手势门控**:`utils/video_gesture_session_gate.dart`(长按 vs 双指缩放仲裁,同一指针序列只有一个所有者)、`widgets/experimental_tap_gateway.dart`(双击探测器,用于隐藏入口)。
- **桌面播放器快捷键**(`utils/desktop_player_shortcuts.dart`,18 项):`Esc` 返回、`Space` 播放/暂停、`←/→` 快退/快进、`S` 设置、`L` 字幕库、`E` 字幕编辑器、`V` 视频合成、`T` 字幕样式、`D` 移动字幕、`B` 字幕侧栏、`F` 全屏、`A` 画面比例、`G` 集数、`P/N` 上/下一集、`C` 字幕开关、`M` 静音;长按空格/方向键触发 2x 加速。
- **媒体管理页快捷键**(`utils/desktop_media_management_shortcuts.dart`,9 项):`Esc` 返回/退出选择、`V` 切换视图、`F` 全屏、`O` 大文件目录、`E` 导出设置、`R` 回收站、`T` 卡片样式、`B` 选择模式、`A` 全选。
- **YouTube 自动字幕归一化**(`utils/youtube_auto_caption_normalizer.dart`):归并 YouTube 滑动窗口式自动字幕(重叠 cue 结束时间裁到下一 cue 起点)。
- **响应式**:`widgets/responsive_icon_button.dart`(按屏宽比例缩放)、`widgets/media_list_layout_metrics.dart`(列表模式几何计算,手机/平板/桌面三档)。

---

## 7. 数据模型与持久化

### 7.1 核心模型(均在 lib/models/)

| 模型 | 要点 |
|---|---|
| `VideoItem` | 约 40 字段:id/path/playbackPath/title/thumbnailPath/durationMs/lastPositionMs/subtitlePath/secondarySubtitlePath/danmakuPath/parentId/isRecycled/type(video\|audio)/codec/sourceFingerprint/sourceRef 等;`toJson` 兼容旧键 `extraSubtitles`,fromJson 含 B 站旧数据迁移推断;派生 getter:`downloadAssociatedSubtitles`/`localSubtitleGroups`/`prefersManagedAssociatedSubtitles` |
| `VideoCollection` | 树形目录:childrenIds(可含视频与子合集)、parentId、isRecycled/recycleTime、sourceRef;fromJson 兼容旧 `videoIds` |
| `MediaSourceRef` | 来源追溯:`kind = url \| bilibiliBv \| bilibiliId` + value/originalValue;挂在 `VideoItem.sourceRef`/`VideoCollection.sourceRef` 上,用于识别"这个视频来自 B 站/URL 下载",驱动重新下载、来源恢复等逻辑 |
| `ManagedSubtitleAsset` | 托管字幕资产:assetId/path/kind(ai\|translated\|imported\|manual\|embedded\|downloaded\|ocr\|cached)/sourceAssetId(生成链)/language/createdAt |
| `SubtitleItem` | 字幕条目:index/startTime/endTime/text/imageLoader(位图字幕懒加载) |
| `PlaybackStateSnapshot` | 播放恢复快照(currentItemId/positionMs/wasPlaying/playlistFolderId/timestamp) |

### 7.2 持久化位置总表

| 数据 | 位置 | 说明 |
|---|---|---|
| 媒体库 | `<数据根>/library.json`(+ `.bak`/`.tmp`) | schemaVersion=2;20s 防抖 + SerialTaskQueue;Windows 可迁移数据根(largeDataRootPath) |
| 应用设置 | shared_preferences(120+ key) | `_RegisteredSetting` 注册表;样式类为 JSON 字符串 |
| 播放状态快照 | shared_preferences key `playback_state_snapshot` | 启动恢复用;进度本体在 VideoItem.lastPositionMs |
| B 站下载任务 | `<appDocDir>/bilibili_download_tasks.json` | 脏快照防抖批量写 |
| yt-dlp 任务 | shared_preferences `yt_dlp_tasks_v1` 等 | 后台 compute 编解码,进度 1s 防抖 |
| 视频合成任务 | `<appDocDir>/compose_tasks.json` | 枚举按 index 持久化(坑) |
| 转写队列 | `<appDocDir>/transcription_queue_cache.json` | 500ms 防抖 |
| B 站 cookies | `<appDocDir>/.bilibili_cookies` | PersistCookieJar |
| 缩略图 | `<数据根>/thumbnails/` | 磁盘文件 + 内存 LRU |
| 兼容音频副本 | `<数据根>/compatible_audio/<sha256>.m4a` | 文件名 = sha256(path\|size\|mtime) |
| 任务字幕 | `<数据根>/subtitles/tasks/<videoId>/` | videoId 仅 `[A-Za-z0-9_-]` |
| OCR 模型物化 | large-data 目录 `ocr_models/ppocr_v4_mobile/` | SHA256 校验 |
| yt-dlp 二进制 | `<ApplicationSupport>/yt_dlp/` | `.version`/`.source` 标记 |
| 崩溃日志 | `<ApplicationSupport>/crash_log.txt` | 1MB 上限,追加写 |

### 7.3 数据迁移约定

- 所有 fromJson 都做缺省值兜底与旧字段迁移(如 `extraSubtitles`→`additionalSubtitles`、`videoIds`→`childrenIds`、旧字幕样式→新三段式样式、旧 `sidebarFontSizeScale`→`landscapeSidebarFontSizeScale`)。
- **新增持久化字段时必须保持向后兼容**:老版本用户数据没有新字段,读取要带 `?? 默认值`;不要改已有枚举成员的顺序/语义(合成任务按 index 存)。

---

## 8. 编码规范与约定(必须遵守)

1. **命名**:文件 snake_case,按 `*_service.dart`、`*_manager.dart`、`*_parser.dart`、`*_model.dart`、`*_screen.dart`、`*_panel.dart`、`*_sheet.dart`、`*_dialog.dart` 后缀分工;类 UpperCamelCase,私有成员 `_` 前缀。
2. **注释**:中文为主;重要不变量、迁移逻辑、平台分支必须写注释说明"为什么"(代码里大量 `///` 文档注释解释兼容性)。
3. **状态管理**:跨页面状态必须放 ChangeNotifier 服务挂 Provider;单例服务用 `factory X() => _instance` 模式;页面内局部状态用 setState;高频更新用 ValueNotifier/ValueListenableBuilder。
4. **竞态防护**:任何异步操作(play/seek/字幕加载/倍速/预加载/发布)必须带请求代际 id,回调先校验;需要严格顺序的异步用 SerialTaskQueue 或命令尾链。
5. **持久化**:先同步改内存 + notifyListeners,再异步落盘;写文件用 `.tmp` + rename + `.bak`;关键保存加防抖;绝不阻塞 UI。
6. **错误处理**:服务层初始化允许失败(catchError + debugPrint);业务错误 try/catch 静默 + `debugPrint('前缀: $e')` 或 `developer.log`;用户可见错误用 `AppToast.show(..., type: AppToastType.error)`。
7. **日志**:`debugPrint`/`developer.log` 带模块前缀(如 `MediaPlaybackService: ...`);kDebugMode 门控开销大的日志;崩溃走 crash_log.txt。
8. **平台分支**:优先 `Platform.isWindows/isMacOS/...` 或 `universal_platform`;桌面/移动 UI 分流在入口处一次性决定(参照 `_buildVideoPlayerRoute`),不要在组件内部散落大量分支。
9. **测试**:新逻辑应配套 `test/xxx_test.dart`(命名与被测文件一致);现有测试大量使用 `@visibleForTesting` 与 `ValueKey`;纯函数优先(现有测试很多直接测纯逻辑)。
10. **绝对禁止**:绕过 `video_player` 接口直接调用 media_kit;在服务中依赖 widget 层(现存唯一例外是 `PlaybackNavigationService` 引用 `portrait_video_screen`,新增代码不要扩大此类依赖);向 `lib/` 外写业务数据(所有持久化走 §7.2 的既定位置)。

---

## 9. 关键流程速查

- **播放**:UI → `MediaPlaybackService.play(item)` → 兼容文件检查(音频转码)→ `VideoPlayerController.file(playbackFile)` → media_kit 适配器 `Player.open` → seek 恢复进度(优先 startPosition > ProgressTracker > item.lastPositionMs,末尾 500ms 归零)→ play;进度每 5s 落盘 + 快照,暂停/切集/seek 后 500ms 立即落盘;播放列表自动装载同文件夹(`loadFolderPlaylist`)。
- **B 站下载**:解析 URL → 取视频/番剧信息 → WBI 签名取 playurl(DASH)→ 分片下载(断点续传)→ ffmpeg 合并 + 时长/兼容性校验(必要时 `_repairVideo`)→ 弹幕转 ASS 边车 → 导入媒体库 → 后处理串行队列。
- **YouTube 下载**:提取 URL → resolveYoutubeMeta → 格式选择 → RequestBuilder 生成参数 → MethodChannel 交原生 yt-dlp(Windows/macOS 进程、Android 内嵌 Python)→ EventChannel 进度事件 → 完成后导入媒体库/挂字幕。
- **OCR 字幕**:选区域 → 抽帧(ffmpeg crop/fps/mpdecimate)→ Isolate 内 ONNX 推理(变化检测+样式学习)→ 合并时间轴 → 导出 SRT → 注册托管字幕。
- **AI 转写**:抽取音频(ffmpeg)→ Bcut ASR 分块上传+轮询 → SRT 落盘 → 注册托管字幕(kind: ai)。
- **视频合成**:请求 → 探测媒体信息 → 字幕加载 → 渲染(预览:实时 widget;精确:离屏 PNG+ffconcat;近似:ASS)→ ffmpeg 执行 → 校验输出 → 清理中间产物。
- **启动恢复**:library 初始化完成 → `restorePlaybackState` 读快照 → 恢复播放列表与位置 → `play(autoPlay: false)`。

---

## 10. 已知问题、坑与待清理项(开发前必读)

**调试残留(应清理)**:
1. `LibraryService._reportDebugEvent` 与 `BilibiliDownloadService._reportDebugEvent`:kDebugMode 下向 `http://127.0.0.1:7777/event` POST 调试事件(sessionId=bilibili-export-crash);`media_duration_probe.dart` 同款残留;B 站服务内还有 `#region debug-point` 注释块。
2. `media_playback_service.dart` 约 2365–2390 行保留一段被注释的遗留代码。

**死代码**:
3. `services/bilibili/grpc_models.dart`:手写 Protobuf 模型,全库无引用(旧版字幕抓取遗留)。
4. `services/ffmpeg_service.dart`:`FFmpegService` 全项目无人 import;`mergeVideo` 还用了硬编码未转义命令且 Windows 不支持。请改用 `FFmpegUtils` + ffmpeg_kit。
5. yt-dlp 服务中 3 处 `// ignore: unused_element` 的 Android staged 路径(`_createAndroidTempArtifactKey/_buildAndroidStagedRequest/_finalizeAndroidTask`)已部分废弃,仅作参考。

**平台风险**:
6. **macOS Release entitlements 只有 `app-sandbox`,缺 `com.apple.security.network.client` 与 user-selected 文件读写**——沙盒下 yt-dlp 联网下载与访问用户文件可能失败(Debug 有 network.server+allow-jit 是为本地调试端口)。macOS Info.plist 还误置了 iOS 键 `UIBackgroundModes`(无效)。
7. **Windows 无代码签名 + 落地运行 yt-dlp.exe/ffmpeg.exe**:易被杀软误报。
8. **iOS 上 yt-dlp 被原生层硬编码禁用**(`YT_DLP_UNSUPPORTED`),UI 若开放下载入口会导致失败。
9. **Android 仅 arm64-v8a**;声明了 `MANAGE_EXTERNAL_STORAGE`(Play 上架需审核)与 `usesCleartextTraffic=true`;`settings.gradle.kts` 用了阿里云/腾讯镜像、`gradle.user.home=D:/.gradle`(换机需注意);`openFileManager` 硬编码 B 站目录在 Android 11+ 分区存储下大概率失效。
10. 下载目录打开、Windows 大目录迁移等路径逻辑分散(`MainActivity.openFileManager` 硬编码 B 站下载目录、`SettingsService.largeDataRootPath` 迁移),改路径相关代码前先 grep `largeDataRootPath`、`_dataRootDir`、`resolveLargeDataRootDir`。

**逻辑坑**:
11. `download_manager._episodeResumeFilesExist` 末尾恒返回 false(疑似未完成逻辑)。
12. `VideoComposeTaskState` 枚举按 `index` 持久化——**不得重排/插入枚举成员**。
13. PGS 解析为简化实现:时间轴每帧默认 +2s、仅取 objects.first、WDS 与 crop 未处理。
14. yt-dlp 内置版本硬编码为 `2026.06.09`(未来日期,非真实版本号);Android 固定输出目录 `yt_dlp_downloads`、忽略自定义 outputDirectory。
15. 必剪 ASR 是无鉴权私有接口,随时可能失效;`_queryTaskResult` 4 分钟抛超时但外层 30 分钟,长音频可能误报。
16. B 站服务复用 `downloadSpeed/downloadSize` 字段承载文本提示,语义易混淆。
17. `LibraryService.getContents` 返回 `List<dynamic>`(需强转);library.json 写失败被静默吞。
18. 大量 `catch (_) {}` 静默吞异常(尤其 yt-dlp 服务),排障困难。
19. 桌面 `Process.start` 同时用 List 参数与 `runInShell: true` 的场景存在含空格/中文路径被二次拆分的风险(视频合成报告指出)。

---

## 11. 后续开发指引

### 11.1 修改前必读

1. 本文件的 §6 对应子系统章节,以及该子系统的核心服务文件头部注释。
2. 查清目标功能涉及哪些持久化数据(§7.2),确认是否需要迁移兼容。
3. grep 所有引用(`Provider.of<X>`、import),确认改动影响面——尤其 `FFmpegUtils`、`LibraryService`、`SettingsService`、`MediaPlaybackService` 属于高扇出模块。

### 11.2 常见任务接入点

| 需求 | 接入点 |
|---|---|
| 新增播放器按钮/手势 | `widgets/video_controls_overlay.dart` + 对应屏幕的 onXxx 回调;桌面快捷键注册 `utils/desktop_player_shortcuts.dart` |
| 新增页面 | `screens/` 新建,经 `home_screen` FAB(`video_action_buttons.dart`)或侧边栏入口跳转;路由命名用 `RouteSettings(name:)` 供 `AppToast.isCurrentRoute` 判定 |
| 新增设置项 | `SettingsService._createRegisteredSettings()` 注册(key+默认值+normalize+apply)+ `settings_panel.dart`/对应 sheet 加 UI |
| 新增字幕来源/格式 | `utils/subtitle_parser.dart`(文本)或参考 `pgs_parser.dart`(位图);接 `MediaPlaybackService.setSubtitles` 管线 |
| 新增下载任务类型 | 参考 `bilibili_download_service` 或 `yt_dlp_download_service` 的"任务模型+队列+节流通知+JSON 持久化+启动修正"模式 |
| 新增 ffmpeg 命令 | 一律走 `utils/ffmpeg_utils.dart`(桌面 CLI/移动 FFmpegKit 双路径),不要新造命令拼接 |
| 新增 OCR/AI 能力 | 参考 `ocr_processing_worker.dart` 的 Isolate 模式与 `transcription_manager.dart` 的任务状态机 |

### 11.3 必须遵守的红线

- 不绕过 `video_player` 接口直接调 media_kit;
- 不修改 `build/`、`windows_backup/`、`apk_analysis/` 等生成/备份目录;
- 新持久化字段向后兼容(§7.3);
- 异步操作带请求代际防竞态(§8.4);
- 所有用户可见错误用 AppToast,所有静默失败至少 debugPrint;
- 平台差异在入口处分流,保持组件内平台分支最小化;
- 改完跑 `flutter analyze` 与相关 `flutter test`(61 个测试文件覆盖了解析器、队列、模型迁移、UI 逻辑等纯逻辑部分)。

---

*文档生成方式说明:本文由对源码的直接静态分析生成(151 个 lib 文件逐一或抽样阅读 + 9 个子系统专项分析 + 平台工程配置核查),未参考项目内任何既有 Markdown/文本文档。*
