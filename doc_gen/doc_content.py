# -*- coding: utf-8 -*-
"""
Fluent Player 项目解析文档 —— 结构化内容稿
块类型：
  ("h1", 文本)            一级标题（章）
  ("h2", 文本)            二级标题（节）
  ("h3", 文本)            三级标题（小节）
  ("p", 文本)             段落（支持 **粗体** 标记）
  ("bullet", 文本)        无序列表项
  ("num", 文本)           有序列表项（自动编号）
  ("code", 文本)          代码块
  ("table", 表头列表, 行列表)
  ("note", 文本)          提示框
  ("quote", 文本)         引用/示例对话
  ("pagebreak",)          强制分页
"""

TITLE = "Fluent Player 跨平台视频播放器 —— 项目全解"
SUBTITLE = "面向零基础读者的完整项目解析 · 读懂它 · 讲清它 · 学会它"

BLOCKS = [

# =====================================================================
# 开篇
# =====================================================================
("h1", "开篇：这份文档是做什么的"),

("p", "这个项目是一款名叫 **Fluent Player** 的跨平台视频播放器 App，由 AI 辅助开发，代码量约 12 万行。你现在不懂代码，但想靠这个项目争取计算机专业的拔尖班名额——那么你不需要把它全部背下来，而是需要做到三件事："),
("num", "**听懂**：这个 App 有哪些功能，每个功能大致是怎么实现的；"),
("num", "**讲清**：面试官问起任何一个功能，你能用大白话讲出思路，并指到具体的代码文件；"),
("num", "**学到**：从这个项目里提炼出通用的编程知识、算法思维、工程习惯，这些才是面试真正考察的东西。"),
("p", "所以这份文档不讲高深术语，而是把每个概念都掰开揉碎，配上项目里的真实例子。你会先补上看懂代码所需的基础知识，再逐层认识项目的骨架和肌肉，最后我会把藏在代码里的算法、工程素养、面试问题全部整理给你。"),
("note", "阅读建议：不要一口气读完。先把第一部分和第七部分过一遍（建立全局印象 + 知道面试要考什么），再回头细读中间的章节。每一节都独立成篇，随时可以跳着看。"),
("pagebreak",),

# =====================================================================
# 第一部分
# =====================================================================
("h1", "第一部分　先认识你的作品"),

("h2", "1.1 用一句话介绍这个 App"),
("p", "Fluent Player 是一款 **跨平台本地媒体播放器**：它既能像普通播放器一样播放电脑/手机里的视频和音频，还能下载 B 站和 YouTube 的视频、自动加载和翻译字幕、渲染弹幕、把语音转成文字、识别画面里的字幕、甚至把字幕“烧”进视频里。"),
("p", "一句话概括它的与众不同：**别的播放器只是“放”，它还能“下载、翻译、转写、识别、合成”**——相当于把五六个独立 App 的功能塞进了一个播放器里。"),
("p", "支持的平台：Android（手机）、iOS（苹果手机）、Windows（电脑）、macOS（苹果电脑），另外代码里保留了 Linux 支持，网页端只是个空壳模板。"),

("h2", "1.2 主要功能清单（大白话版）"),
("table", ["功能", "大白话解释", "对应核心文件"],
 [
  ["媒体库管理", "把你电脑/手机里的视频整理成“书架”，可以建文件夹、排序、拖拽、批量导入、看缩略图，删了还能进回收站找回来", "library_service.dart、home_screen.dart"],
  ["视频/音频播放", "支持几乎所有常见格式，能续播、倍速、快进快退、调整画面比例、横竖屏切换、播放上一集/下一集", "media_playback_service.dart、video_player_screen.dart"],
  ["字幕系统", "自动识别视频旁边的字幕文件、支持中英双语、五种引擎在线翻译、能改字体样式、能拖动字幕位置、还能编辑字幕", "subtitle_parser.dart、subtitle_translation_service.dart"],
  ["弹幕", "下载 B 站弹幕，像看 B 站一样在视频上飘弹幕，样式可调", "bilibili_danmaku_ass.dart、danmaku_overlay.dart"],
  ["B 站下载", "粘贴 B 站视频链接就能下载，支持登录、多集下载、断点续传、自动合并音视频", "bilibili_download_service.dart"],
  ["YouTube 下载", "内置了业界著名的 yt-dlp 下载工具，粘贴链接就能下，支持画质选择、批量下载", "yt_dlp_download_service.dart"],
  ["视频合成", "把字幕“印”进视频画面里，生成一个新视频文件（硬字幕），或把字幕封装成可开关的软字幕", "video_compose_manager.dart"],
  ["AI 语音转文字", "把视频里的对话转成文字字幕，相当于自动配字幕（调用 B 站必剪的语音识别接口）", "transcription_manager.dart"],
  ["OCR 识别硬字幕", "识别视频画面里“印死”的中英文字幕（比如字幕组压进画面的），框选区域后自动抽帧识别并生成字幕文件", "ocr_subtitle_manager.dart"],
  ["系统集成", "手机通知栏控制播放、锁屏控制、耳机按键、电脑全屏/快捷键、拖文件进窗口直接导入", "system_media_session_service.dart"],
 ]),

("h2", "1.3 这个项目凭什么值得讲（技术亮点）"),
("p", "面试官不关心你的 App 功能多不多，他关心的是**难度和技术含量**。这个项目有几个硬核点，够你在面试里反复使用："),
("bullet", "**跨平台**：一套 Dart 代码同时跑 Windows / macOS / Android / iOS 四个平台，涉及平台差异适配、原生代码桥接（MethodChannel）。"),
("bullet", "**自研算法**：字幕解析器、弹幕泳道分配、字幕时间轴二分查找、缓存 LRU 淘汰、时序竞态防护——全是自己写的，不是调库。"),
("bullet", "**AI 能力**：本地跑神经网络模型做 OCR（PaddleOCR ONNX），调云端接口做语音转文字，涉及异步任务队列、断点续传。"),
("bullet", "**工程复杂度**：12 万行代码、90 个测试文件、多种设计模式（单例、工厂、观察者、依赖注入）、严谨的错误处理和崩溃日志。"),
("bullet", "**性能优化**：缩略图多级缓存、弹幕纹理图集、后台 Isolate 并行处理、播放时间线插值防抖动。"),
("note", "面试话术可以这样说：\"我的项目最大的挑战，是把很多原本需要专业软件才能做到的能力（字幕翻译、弹幕渲染、视频下载、OCR、语音转写）集成到一个播放器里，同时保证 12 万行代码不乱、不卡、不崩溃。这逼着我学习了很多工程化手段。\""),

("h2", "1.4 项目规模与基本档案"),
("table", ["项目", "数值", "说明"],
 [
  ["应用名", "Fluent Player", "代码里 MaterialApp 的 title 是 Fluent_Player"],
  ["Dart 包名", "video_player_app", "工程文件夹名，用于区分包"],
  ["版本号", "1.0.0+2", "主版本.次版本.修订+构建号"],
  ["Dart SDK", "^3.8.1", "编程语言版本"],
  ["代码规模", "176 个 Dart 文件，约 12 万行", "最大文件 library_service.dart 约 5850 行"],
  ["测试", "90 个测试文件", "覆盖解析器、队列、模型迁移等纯逻辑"],
  ["目标平台", "Android / iOS / Windows / macOS", "Linux 保留支持，Web 仅模板"],
  ["核心依赖", "约 40 个第三方库", "播放、网络、AI、UI 各领域都有"],
 ]),
("p", "这段数字你最好背下来。面试时主动报出“12 万行、90 个测试”会立刻让面试官对你的项目体量有概念。"),

# =====================================================================
# 第二部分
# =====================================================================
("pagebreak",),
("h1", "第二部分　看懂代码前的地基：必备基础知识"),

("h2", "2.1 计算机是怎么“运行”程序的"),
("p", "想象一个厨师（CPU 中央处理器）站在厨房里，面前有一本写满步骤的菜谱（程序代码）。菜谱说“打两个鸡蛋”，厨师就去拿鸡蛋、打蛋；说“热锅”，厨师就去开火。**程序就是一本写给计算机看的菜谱**，里面的每一行都是“指令”。"),
("p", "菜谱（代码）会用到两种东西："),
("bullet", "**数据**：比如“鸡蛋的数量 = 2”“油的温度 = 180 度”，数据要放在“临时小桌板”（内存）或“储物柜”（硬盘文件）里；"),
("bullet", "**逻辑**：比如“如果油温到了 180 度，就下锅；否则继续等”，这就是所谓的“判断”“循环”“函数调用”。"),
("p", "你项目里的每一行 Dart 代码，最终都会被翻译成计算机能懂的指令，然后由 CPU 一条条执行。编程的本质，就是**用精确的语言描述一个复杂流程**，让机器替你干活。"),

("h2", "2.2 编程语言是什么，Dart 又是什么"),
("p", "计算机只懂 0 和 1，人没法直接用 0/1 写大程序，所以发明了“编程语言”——一种介于人话和机器码之间的精确语言。"),
("p", "**Dart** 就是 Google 发明的这样一门语言，它是这个项目的“母语”。语法长这样："),
("code", """// 定义一个函数：计算两个数相加
int add(int a, int b) {
  return a + b;
}

// 定义一种“类型”，类似模具
class VideoItem {
  String title;   // 视频标题
  int durationMs; // 时长（毫秒）
}"""),
("p", "你不需要现在学会写 Dart，只需知道：代码里的英文单词（`class`、`int`、`return`）是语言的关键字，而 `VideoItem`、`title` 这些是开发者（你和 AI）自己起的名字。整个项目就是由这种“关键字 + 自定义名字”组合出来的。"),

("h2", "2.3 Flutter 是什么，为什么一套代码能跑四个平台"),
("p", "做手机 App 有很多办法：安卓用 Kotlin，苹果用 Swift，电脑用 C#……不同平台语言不同，等于同一个 App 要写四遍。"),
("p", "**Flutter** 是 Google 出的一个“神奇画布”框架：你用一套 Dart 代码描述界面，Flutter 自己在每个平台上把界面“画”出来，而且画出来的是原生级别的流畅效果。这样**一套代码，四处运行**。"),
("p", "原理上可以这样理解：Flutter 像一个自带画笔和画布的画家，不管你家墙（Windows）还是玻璃墙（iOS），它都用同一套手法在上面作画。缺点是“画布”需要一点额外内存，优点是你不用为每个平台各学一门语言。"),
("note", "面试常问：\"Flutter 和原生开发有什么区别？\"答案：原生是直接用平台自己的工具（Android 的 Kotlin、iOS 的 Swift），性能最好但每平台写一遍；Flutter 用一套 Dart 代码跨平台，开发效率高、界面一致，代价是包体积更大、某些底层能力需要原生代码配合。"),

("h2", "2.4 认识 Flutter 的“积木”：Widget"),
("p", "在 Flutter 里，**界面上的一切都是 Widget（小部件/积木）**。按钮是积木，文字是积木，整个页面也是一块大积木，里面套着很多小积木。就像搭乐高。"),
("p", "举个项目里的真实例子，播放按钮的图标大概是这样写的："),
("code", """Icon(
  Icons.play_arrow,   // 播放箭头图标
  size: 48,           // 大小 48 像素
  color: Colors.white,// 白色
)"""),
("p", "页面最外层的“大积木”叫 `Scaffold`（脚手架），它提供了背景、顶栏、底部等布局骨架。你在 App 里看到的每一帧画面，都是一棵“积木树”：外层套内层，内层再套更小的。"),
("p", "界面的另一件大事叫 **状态（State）**。比如播放/暂停按钮：程序需要记住“现在是在播放还是暂停”，然后据此画出不同的图标。Flutter 用 `StatefulWidget` 表示“有状态的界面”，状态一变，界面自动重画。你项目里的播放页就是典型的带状态页面。"),

("h2", "2.5 App 的经典三层：界面 / 业务 / 数据"),
("p", "成熟的项目都不会把代码堆成一坨，而是分成三层，各干各的："),
("bullet", "**界面层（UI）**：只管“长什么样、按钮在哪、点击后调用谁”。在你的项目里对应 `screens/`（页面）和 `widgets/`（组件）两个文件夹。"),
("bullet", "**业务层（Services）**：负责“干正事”——播放、下载、解析字幕、翻译、OCR。对应 `services/` 文件夹。"),
("bullet", "**数据模型层（Models）**：定义“数据长什么样”——比如一个视频包含哪些信息。对应 `models/` 文件夹。"),
("p", "打个比方：餐厅里，界面层是服务员（接待客人），业务层是后厨（炒菜），数据层是仓库（存食材）。服务员不直接进仓库翻东西，而是下单给后厨；后厨按菜单取用仓库的食材。这样任何一层改动，都不会连累其他层。"),

("h2", "2.6 什么是“第三方库”（依赖）"),
("p", "编程界有个规矩：**不要重复造轮子**。别人写好的、被广泛验证过的代码，打包成“库”，你在项目里声明“我要用这个库”，就能直接调用它的功能。"),
("p", "这些声明写在项目根目录的 `pubspec.yaml` 文件里。你的项目用了几十个库，举几个例子："),
("bullet", "`video_player`：播放视频的官方接口（你在代码里调它，它底层再去调各平台的播放引擎）；"),
("bullet", "`media_kit`：基于 libmpv 和 FFmpeg 的跨平台播放引擎，解码能力超强；"),
("bullet", "`dio`：发网络请求的库，B 站/YouTube 下载全靠它；"),
("bullet", "`onnxruntime_v2`：运行 AI 模型的库，OCR 靠它；"),
("bullet", "`provider`：状态管理库，让不同页面共享同一个数据；"),
("bullet", "`ffmpeg_kit_flutter_new`：调用 FFmpeg 命令行工具处理视频音频。"),
("p", "用第三方库就像开饭店不用自己种菜，去菜市场买。真正的功力在于**知道该买什么菜、怎么把菜组合成一道菜**——也就是架构和业务逻辑，这部分是你项目里自己写的核心价值。"),

("h2", "2.7 什么是“平台通道”：为什么手机和电脑代码不一样"),
("p", "Flutter 虽然跨平台，但有些能力只有系统底层才有，比如“读手机相册”“控制系统音量”“弹出系统通知”。这时候 Dart 代码没法直接做，需要**借原生平台的力**。"),
("p", "沟通的方式叫 **MethodChannel（方法通道）**：Dart 这边发一条消息“帮我打开文件选择器”，Android/Windows 的原生代码（Java/Kotlin/C++）收到后干活，再把结果传回来。你的项目里多处用到："),
("bullet", "`windows_video_player_media_kit.dart`：把 media_kit 播放引擎“注册”成 video_player 的底层实现；"),
("bullet", "`yt_dlp_native_bridge.dart`：Windows/macOS 原生代码负责运行 yt-dlp 进程，进度通过 EventChannel 回传；"),
("bullet", "Android 的 `MainActivity`、macOS 的 `AppDelegate`：处理文件分享、打开方式等系统能力。"),
("p", "这就是为什么项目里除了 `lib/`（Dart 代码），还有 `android/`、`windows/`、`ios/`、`macos/` 这些文件夹——它们是各平台的“原生小助手”。"),

("h2", "2.8 数据存在哪里：内存、文件、数据库"),
("p", "程序产生的数据有三个去处："),
("bullet", "**内存（RAM）**：速度快、断电就没了，用来放“当前正在用的东西”，比如正在播放的视频信息。"),
("bullet", "**文件**：长期保存，比如 `library.json`（你的媒体库清单）存在磁盘上，重启 App 还能读回来。"),
("bullet", "**数据库**：结构化存储，适合大量数据。你的项目主要用 JSON 文件和 shared_preferences（一种轻量键值存储），没用传统数据库。"),
("p", "你项目里的数据存储方式正好是很好的面试素材：媒体库存成 `library.json`，设置项存在 `shared_preferences`，播放进度也落盘，下载任务存在各自的 JSON 文件里。"),
("p", "有一个细节非常专业：写 `library.json` 时，代码不会直接覆盖原文件，而是**先写一个临时文件 `.tmp`，再改名替换，再留一个 `.bak` 备份**。这样即使写一半断电，原数据也还在。这叫**原子写入**，是工程上防数据损坏的标准做法。"),

("h2", "2.9 什么是“异步”：为什么播放视频不能卡"),
("p", "程序有个特点：很多操作要“等”——等网络返回、等文件读盘、等视频解码。如果干等，界面就会卡住不动。解决办法是**异步**：发出请求后先去做别的事，等结果回来了再接着处理。"),
("p", "Dart 里的异步关键字是 `async`（声明这个函数是异步的）和 `await`（在这里等结果，但不阻塞界面）。举个例子："),
("code", """// 异步下载：不卡界面
Future<void> download() async {
  await http.get(url);   // 等网络，但界面不卡
  print('下载完成');      // 拿到结果后执行
}"""),
("p", "整个项目到处都是异步，最典型的是播放器：视频解码在后台线程跑，界面每帧去读取“现在播放到哪了”，这就是为什么你拖进度条、点暂停时界面依然顺滑。"),

("h2", "2.10 面向对象：类、对象、方法"),
("p", "Dart 是“面向对象”语言。这个概念听起来玄，其实很朴素："),
("bullet", "**类（class）**：一张图纸，描述某种事物的模板。比如 `class VideoItem` 定义了“一个视频有哪些属性”。"),
("bullet", "**对象（instance）**：按图纸造出来的具体东西。比如你媒体库里某部具体的电影，就是一个 `VideoItem` 对象。"),
("bullet", "**方法（method）**：图纸上定义的“这个事物能做什么”，比如 `play()`、`pause()`。"),
("bullet", "**继承 / 组合**：一个类可以基于另一个类扩展（继承），或者把别的类当作自己的零件（组合）。"),
("p", "你项目里的例子：`MediaPlaybackService` 是一个类，全局只有一个它的对象（单例），它能 `play()`、`pause()`、`seekTo()`。UI 层就是不断调用这个对象的这些方法。"),

# =====================================================================
# 第三部分
# =====================================================================
("pagebreak",),
("h1", "第三部分　项目的整体骨架"),

("h2", "3.1 目录结构地图"),
("p", "整个项目分为几大块，先看 `lib/`（核心代码都在这里）："),
("code", """lib/                            # Dart 源码（约 12 万行）
├── main.dart                  # 入口：App 启动时最先执行的代码
├── screens/                   # 页面（13 个）：主页、播放页、下载页……
├── widgets/                   # 可复用组件（37 个）：控制条、字幕层、弹幕层……
├── services/                  # 业务逻辑（49 个）：播放、媒体库、字幕、OCR、B站下载……
│   ├── bilibili/              # B 站下载的子模块（8 个）
│   └── video_compose/         # 视频合成子模块（13 个）
├── models/                    # 数据模型（20 个）：视频、合集、字幕、弹幕……
├── utils/                     # 工具函数（20 个）：解析器、队列、快捷键……
├── platform/                  # 平台适配（把 media_kit 接入 video_player）
└── features/youtube_download/ # YouTube 下载的独立模块

android/ ios/ windows/ macos/ linux/  # 各平台原生工程
assets/                      # 内置资源：字体、FFmpeg、yt-dlp、OCR 模型
test/                        # 90 个测试文件"""),
("p", "这套目录命名本身就有讲究：`*_service.dart` 是业务服务，`*_screen.dart` 是页面，`*_widget.dart` 是组件，`*_model.dart` 是数据模型，`*_parser.dart` 是解析器，`*_utils.dart` 是工具。**看文件名就能猜到文件职责**——这也是工程规范的一种。"),

("h2", "3.2 启动流程：从双击图标到看到主页"),
("p", "任何程序都有一个“入口”，Flutter 里叫 `main()`，在 `lib/main.dart`。启动时它干了一连串事情，按顺序大致是："),
("num", "**配置图片缓存**：限制缓存大小，防止 Windows 内存爆掉（`_configureImageCaches`）；"),
("num", "**创建全局服务对象**：设置、媒体库、播放、字幕、下载、OCR 等 10 多个服务全部创建好；"),
("num", "**等待界面第一帧显示**：先显示一个启动画面（Logo），让用户觉得“开了”，同时后台继续初始化；"),
("num", "**初始化核心服务**：读取设置、初始化播放服务、注册 media_kit 播放引擎、加载媒体库文件；"),
("num", "**恢复上次播放状态**：读上次退出时保存的“播放到哪了”，准备就绪但不自动播放；"),
("num", "**安装崩溃日志**：万一出错，把错误写进 `crash_log.txt` 方便排查；"),
("num", "**把服务挂到界面上**：通过 `MultiProvider` 把全局服务“分发”给所有页面使用；最后挂载主页 `HomeScreen`。"),
("p", "注意一个设计亮点：**轻量启动**。最耗时的初始化（读磁盘、加载媒体库）被放到第一帧显示之后做，用户感觉 App “秒开”。这是移动开发里非常讲究的“启动性能优化”。"),
("note", "你还可以看到它对异常的处理：每个服务的初始化都用 try/catch 包住，某个服务初始化失败不影响其他服务启动，只打印日志。这种“单个失败不拖垮整体”的思想贯穿整个项目。"),

("h2", "3.3 状态管理：Provider 与 ChangeNotifier"),
("p", "一个 App 里有很多“共享状态”：当前播的是哪一集、进度到哪了、音量多少。如果每个页面各存一份，就会出现“这页改了那页不知道”的混乱。"),
("p", "这个项目用 `provider` 库统一管理共享状态。核心机制是："),
("bullet", "**ChangeNotifier**：一个“大喇叭”。服务对象继承它，当内部状态变化时调用 `notifyListeners()`，相当于广播“我变了！”；"),
("bullet", "**Provider**：负责把服务对象“分发”给页面。页面通过 `Provider.of<T>(context)` 拿到服务对象，并监听它的广播，一旦变化就刷新界面；"),
("bullet", "**MultiProvider**：一次挂载多个服务（main.dart 里挂了 11 个）。"),
("p", "举个例子，你拖播放进度条时：进度条组件 `notifyListeners` → 所有监听者收到通知 → 界面重画。整个 App 共享同一个 `MediaPlaybackService` 对象，所以播放页、迷你播放卡片、控制栏看到的进度永远一致。"),
("note", "还有一个精细优化：像“进度条”“歌词”这种每帧都在变的，用 `ValueListenableBuilder` 只刷新自己那一小块，而不是整个页面重画。这属于性能细节，面试提出来很加分。"),

("h2", "3.4 单例模式：全 App 只有一个“播放器大脑”"),
("p", "有些东西，全局只需要一个：播放器（你不可能同时有两个主播放器在放不同视频）、媒体库（数据库不能开两份）。这种“全局唯一”的需求用 **单例模式** 实现。"),
("p", "项目里的写法是“单例工厂”，以 `MediaPlaybackService` 为例（代码简化）："),
("code", """class MediaPlaybackService extends ChangeNotifier {
  // 静态变量：存放唯一的实例
  static final MediaPlaybackService _instance =
      MediaPlaybackService._internal();
  // 私有构造函数：禁止外部 new 新对象
  MediaPlaybackService._internal();
  // 工厂构造：无论调用多少次，都返回同一个 _instance
  factory MediaPlaybackService() => _instance;
}"""),
("p", "这样，任何页面写 `MediaPlaybackService()` 拿到的都是同一个对象，天然共享。面试时如果被问“什么是单例模式”，这就是现成的例子。"),

("h2", "3.5 分层架构：UI / 服务 / 数据"),
("p", "前面讲过 App 的三层。在这个项目里的具体体现是："),
("bullet", "**UI 层**（`screens/`、`widgets/`）：只负责显示和收集用户操作。比如播放页收到“双击”手势后，调用 `MediaPlaybackService().seekTo(...)`，它不关心怎么 seek。"),
("bullet", "**服务层**（`services/`）：负责真正的逻辑。播放服务管状态机、字幕服务管解析、下载服务管网络。"),
("bullet", "**模型层**（`models/`）：定义数据结构，比如 `VideoItem` 记录视频的所有属性，并提供 `toJson()`（存盘）和 `fromJson()`（读盘）两个方法。"),
("p", "层与层之间通过“接口”沟通，互相不知道对方内部实现。这种解耦让 12 万行代码可以多人/多轮 AI 协作而不互相踩脚。"),

("h2", "3.6 数据是怎么被记住的：持久化总表"),
("p", "App 重启后还能记住媒体库、设置、播放进度、下载任务，靠的是“持久化”——把数据写进磁盘。下表是项目里所有数据存放的位置，面试问到“数据怎么保存”可以直接引用："),
("table", ["数据", "存放位置", "说明"],
 [
  ["媒体库清单", "数据根目录/library.json", "自动备份 .bak，写入有防抖"],
  ["应用设置（120+ 项）", "shared_preferences", "键值对，样式类设置存 JSON 字符串"],
  ["播放状态快照", "shared_preferences", "记录上次播放的条目和位置，用于启动恢复"],
  ["B 站下载任务", "appDocDir/bilibili_download_tasks.json", "断点续传状态也在这里"],
  ["YouTube 下载任务", "shared_preferences (yt_dlp_tasks_v1)", "任务和进度存这里"],
  ["视频合成任务", "appDocDir/compose_tasks.json", "枚举按索引持久化"],
  ["语音转写队列", "appDocDir/transcription_queue_cache.json", "重启后恢复任务"],
  ["B 站登录 Cookie", "appDocDir/.bilibili_cookies", "登录态持久化"],
  ["缩略图", "数据根目录/thumbnails/", "磁盘缓存图片"],
  ["崩溃日志", "appDocDir/crash_log.txt", "1MB 上限，追加写"],
 ]),
("p", "一个值得骄傲的工程细节：**数据迁移兼容**。比如旧版本的媒体库字段叫 `extraSubtitles`，新版本改成了更精确的结构，`fromJson` 时会把旧字段自动迁移到新结构，保证老用户升级后数据不丢。软件工程里这叫“向后兼容”，是专业开发者的基本素养。"),

# =====================================================================
# 第四部分
# =====================================================================
("pagebreak",),
("h1", "第四部分　核心功能逐个拆解"),

("h2", "4.1 媒体库：你的“视频书架”"),
("p", "媒体库解决一个问题：**怎么把散落在硬盘各处的视频，整理成一个方便浏览、搜索、播放的目录**。"),
("h3", "4.1.1 数据结构：树"),
("p", "媒体库的内部结构是**树**——有根、有分支、有叶子，就像文件夹套文件夹。"),
("bullet", "`VideoItem`：叶子，代表一部视频，记录路径、标题、时长、缩略图、播放进度、字幕等 40 个属性；"),
("bullet", "`VideoCollection`：分支，代表一个“合集/文件夹”，它的 `childrenIds` 可以装视频，也可以装子合集，从而实现无限层级的文件夹；"),
("bullet", "根级列表：`_rootChildrenIds`，记录最顶层有哪些合集和视频。"),
("p", "所有信息存在内存的 Map（键值对集合）里，改了就 `notifyListeners()` 通知界面刷新。"),

("h3", "4.1.2 保存：防抖 + 串行队列 + 原子写入"),
("p", "用户随便拖一下、改个名就写盘，太频繁；不写又怕丢。项目用了一套组合拳："),
("bullet", "**防抖（debounce）**：连续操作时只启动一个 20 秒的计时器，20 秒内没有新操作才真正写盘。你狂拖排序，它也只在停手 20 秒后保存一次；"),
("bullet", "**串行队列**：写盘任务排队执行，避免两个写盘同时进行互相覆盖；"),
("bullet", "**原子写入**：先写 `.tmp` 再改名，留 `.bak` 备份。"),
("p", "这三个词（防抖、串行队列、原子写入）都是工程上的硬通货，面试可以展开讲。"),

("h3", "4.1.3 缩略图：两级缓存"),
("p", "给每个视频生成一张封面图，是件很贵的事（要解码视频抽帧）。项目把缩略图存成磁盘文件，同时把常用的放在内存缓存里，内存里用的是 **LRU**（最近最少使用）策略：超过上限就把最久没用的挤出去。"),
("p", "还有一个“预加载”机制：当你往下滚动列表时，程序预判你接下来会看到哪些视频，提前把它们的缩略图加载进内存，滚动时就感觉不到卡顿。"),

("h3", "4.1.4 回收站：软删除"),
("p", "删除视频时，项目不会真的删文件，而是打个标记（`isRecycled = true`）。这样用户随时能从回收站恢复。真正“彻底删除”时，只删除应用自己管理的副本文件，绝不动用户硬盘上的原始文件——**这是对用户数据安全的敬畏**。"),

("h3", "4.1.5 批量导入"),
("p", "支持一次导入整个文件夹、甚至导入 zip 压缩包（自动解压并按目录结构整理）。这类耗时的扫描、解压工作被放到后台 Isolate 里做，避免界面卡死。还专门做了**路径穿越防护**（防止压缩包里的文件名带 `..` 逃出解压目录），这是安全意识很强的细节。"),

("h2", "4.2 视频播放：最核心的一环"),
("h3", "4.2.1 播放是怎么“接上”的"),
("p", "项目的播放链路是：**UI → video_player 接口 → media_kit 播放引擎 → libmpv/FFmpeg 解码 → 画面显示**。"),
("p", "有意思的是，代码里 UI 用的还是标准的 `video_player` 接口，但项目在底层写了一个“适配器”（`windows_video_player_media_kit.dart`），把 `video_player` 的底层实现替换成了 `media_kit`。这样做的好处是："),
("bullet", "所有平台统一用 media_kit（基于 FFmpeg），解码格式不受系统限制，MKV、FLAC、各种编码通吃；"),
("bullet", "上层代码完全不用改——因为接口没变，只是换了个“发动机”。"),
("p", "这种“换发动机不换方向盘”的思想叫**适配器模式**，是设计模式的一种，面试能讲这个很加分。"),

("h3", "4.2.2 播放状态机"),
("p", "播放器内部维护一个状态：`idle`（空闲）→ `loading`（加载中）→ `playing`（播放中）↔ `paused`（暂停），出错则进入 `error`。所有操作（播放、暂停、快进）都受这个状态机约束，避免“还没加载完就播放”之类的混乱。"),

("h3", "4.2.3 竞态防护：最体现功力的地方"),
("p", "想象你快速点了 10 次“下一集”：网络很慢，第 1 集的加载还没回来，你已经发出第 10 集请求了。等第 1 集的结果回来时，如果代码傻傻地执行“播放第 1 集”，就乱套了。"),
("p", "项目用一个很聪明的办法：**代际计数（generation id）**。每次发起播放请求，就把计数器 `_playRequestId` 加一，异步结果回来时先检查自己的编号是否还是最新的，不是就丢弃。代码里 `_isCurrentPlayRequest(requestId, ...)` 干的就是这件事。"),
("p", "这是并发编程里最核心的思维——“过期结果不能执行”，面试官听到“我用代际计数防止异步竞态”会眼前一亮。"),

("h3", "4.2.4 断点续播与进度保存"),
("p", "播放进度每 5 秒落盘一次，暂停、切集、快进后立即落盘。退出 App 后重新打开，能恢复到上次的位置。细节：如果上次已经播到结尾（末尾 500 毫秒内），进度归零，因为“播完的视频应该从头看”。"),

("h3", "4.2.5 预加载下一集"),
("p", "当播放进度超过 80% 时，程序就在后台偷偷初始化“下一集”的播放器。等你点“下一集”时，几乎是瞬间切换。旧播放器不会立刻销毁，而是在后台慢慢释放（最多同时 3 个），防止内存泄漏。"),

("h3", "4.2.6 时间轴：让进度条和弹幕不抖动"),
("p", "视频播放时，底层解码器会频繁报告“现在播到哪了”，但这些采样有时会轻微抖动。项目写了一个 `PlaybackTimelineClock`（时间线时钟），用“推算 + 微调”的方式维持一个平滑的时间轴："),
("bullet", "界面每帧（1/60 秒）按播放速度推算出当前位置，这叫**插值**；"),
("bullet", "偶尔和底层的真实位置对比，如果偏差很小，就通过微调“斜率”（每分钟最多修正 0.5%）悄悄追平，不让弹幕突然跳一下；"),
("bullet", "如果偏差很大（比如用户拖了进度条），就认为是“跳跃”而不是“漂移”，直接重置时钟。"),
("p", "这种“用插值保证平滑、用漂移校正保证准确”的思路，是很多实时系统（游戏、直播、动画）的通用手法。"),

("h2", "4.3 字幕系统：五大能力"),
("h3", "4.3.1 解析：读懂各种字幕文件"),
("p", "字幕文件有几种常见格式：SRT、VTT、ASS、LRC，内容大同小异——都是“时间点 + 文字”。项目自己写了解析器（`subtitle_parser.dart`），把文本拆成一条条的 `SubtitleItem`（开始时间、结束时间、文字）。"),
("p", "第一个难题是**编码**：字幕文件可能是 UTF-8、UTF-16、GBK 等不同编码存的，直接按 UTF-8 读会乱码。项目的解法是先看文件头（BOM 标记），没有标记就用“零字节统计法”猜 UTF-16，再试 UTF-8，不行试 GBK——层层试探，像开锁一样。"),
("p", "更难的是 **PGS 位图字幕**（电影蓝光原盘用的 .sup 格式）：字幕不是文字，是一张张图片。项目实现了 RLE 解码、把颜色空间从 YCbCrA 转成 ARGB、生成 BMP 图片，再用后台计算线程渲染。这属于专业级播放器才会做的功能。"),

("h3", "4.3.2 定位：二分查找"),
("p", "播放时每帧都要问“当前时间该显示哪条字幕？”如果字幕有一万条，从第一条挨个找就太慢了。项目把字幕按开始时间排好序，用**二分查找**，几十次比较就能定位。这个算法单独放第五节详细讲。"),

("h3", "4.3.3 自动发现与双语"),
("p", "打开一个视频，程序会自动扫描同目录，找文件名相近的 .srt/.ass 等字幕文件（`subtitle_file_matcher`）；还会探测视频内嵌的字幕轨道。如果中英两份字幕都存在，还能自动配对成“双语字幕”上下显示。"),

("h3", "4.3.4 在线翻译：五引擎"),
("p", "翻译功能调用 5 个在线翻译服务：Google、必应、360、MyMemory、Reverso。每个都是“发请求 → 拿结果”。翻译是耗时操作，所以做成独立任务队列，页面关了任务还能在后台继续，重启 App 也能恢复。"),

("h3", "4.3.5 样式与编辑"),
("p", "用户可以给中文字幕和英文字幕分别选字体、颜色、描边、阴影；字幕位置可以拖到屏幕任意位置并吸附；还有一整套字幕编辑器，支持实时改文字、撤销重做（undo/redo 用“快照栈”实现，最多存 500 步）。"),

("h2", "4.4 弹幕：会动的评论"),
("h3", "4.4.1 从 B 站 XML 到可以渲染的 ASS"),
("p", "B 站弹幕的原生格式是 XML（一种标记语言）。项目先把 XML 解析出来（正则匹配 `<d ...>文字</d>`），拿到每条弹幕的**时间、类型（滚动/顶部/底部）、颜色、文字**。"),
("p", "然后把它们转成 ASS 字幕格式（`bilibili_danmaku_ass.dart`），转的时候要做一件很关键的事——**泳道分配**：把同一时间出现的弹幕分配到不同“车道”（y 坐标），避免重叠。算法细节在第五部分讲。"),

("h3", "4.4.2 渲染：性能是王道"),
("p", "弹幕多的时候每秒可能几百条，如果每条都慢慢画，肯定卡。项目的做法是："),
("bullet", "把常用文字先“画”成纹理，存进一个 2048×1024 的图集（atlas），画的时候直接贴纹理，省去重复排版；"),
("bullet", "图集按 LRU 管理，内存预算移动端 64MB、桌面 128MB；"),
("bullet", "一个“性能调速器”监测每帧耗时，如果太慢就自动降级（比如减少预渲染量、降低画质）；"),
("bullet", "按当前播放时间预取接下来 5 秒的弹幕纹理，避免边播边卡。"),
("p", "这种“空间换时间、监测性能并自适应”的优化思路，是游戏和视频领域的通用做法。"),

("h2", "4.5 B 站下载：会签名、会断点续传的下载器"),
("p", "这个模块是项目里最有“技术含量”的部分之一，因为它**没有调用现成下载软件，全是自己实现的**。流程是："),
("num", "**解析链接**：粘贴一个 B 站链接，程序用正则提取出视频 ID（BV/av/ep/ss），识别出是普通视频还是番剧（正则代码在 `bilibili_url_parser.dart`）；"),
("num", "**请求视频信息**：调用 B 站的公开接口拿视频标题、分P列表、清晰度等；"),
("num", "**WBI 签名**：B 站的取流接口要求参数签名，项目实现了官方的 WBI 签名算法——从接口拿到两个密钥，打乱顺序拼成一个“mixin key”，加上时间戳，把所有参数排序后拼起来做 MD5 哈希，生成签名（代码在 `wbi_signer.dart`）；"),
("num", "**登录**：支持扫码或粘贴 Cookie（SESSDATA），登录后能下载更高画质和会员内容；"),
("num", "**DASH 分片下载**：B 站的高清视频把画面和声音分开存（视频流 + 音频流），下载器把两者分别下下来，并支持断点续传——中断后从上次的位置接着下，用 HTTP 的 `Range` 头实现；"),
("num", "**合并**：用 FFmpeg 把视频流和音频流合成一个完整文件；"),
("num", "**校验与修复**：检查合成文件时长是否和预期一致，不一致就自动转码修复；"),
("num", "**下载弹幕并导入媒体库**：把弹幕转成 ASS 存到视频旁边，再把视频登记进媒体库。"),
("p", "这个模块展示了完整工程素养：任务状态机（排队/下载中/合并中/失败/完成）、并发控制（Windows 最多同时 2 个任务）、进度节流通知、任务持久化（重启后恢复）、看门狗超时（防止任务卡死）。"),
("note", "WBI 签名本质是“消息认证码”：用密钥把参数算出一个指纹，服务器验证指纹就知道请求是“真的”。它用到了**哈希函数**和**参数排序规范化**，这些是计算机安全的入门概念，讲出来很显专业。"),

("h2", "4.6 YouTube 下载：内置一个 yt-dlp"),
("p", "YouTube 下载更复杂，因为 YouTube 的反爬机制极其多变。项目选择“站在巨人的肩膀上”：内置了 **yt-dlp**——全球最流行的视频下载命令行工具。"),
("bullet", "**二进制打包**：把 yt-dlp.exe（Windows）和 ffmpeg 一起打包进应用，运行时解压到用户目录，还支持在线升级 yt-dlp；"),
("bullet", "**原生进程桥接**：Windows/macOS 上，Dart 通过 MethodChannel 让原生代码启动 yt-dlp 进程，实时解析它的输出，把进度通过 EventChannel 回传给界面；Android 没有独立进程，改用内嵌 Python（Chaquopy）运行 yt-dlp；"),
("bullet", "**智能重试**：yt-dlp 失败时，程序会分析失败类型，自动换更“保守”的配置重试（最多 10 次，逐步退避：降并发、加超时、换 player_client、启用 Cookie、挂代理……），每次重试前等待 1~6 秒；"),
("bullet", "**画质选择器**：根据用户偏好（比如“720p、优先 mp4/h264”），从 yt-dlp 返回的格式列表里挑最合适的，必要时把最佳视频流和最佳音频流合并。"),
("p", "这个模块体现的是**务实**：承认“造轮子成本太高”，选择集成成熟工具，同时做好桥接、进度、错误恢复。面试时可以讲“我学会了如何集成外部命令行工具，并处理跨平台差异”。"),

("h2", "4.7 视频合成：把字幕“烧”进视频"),
("p", "“烧字幕”就是把字幕变成视频画面的一部分，生成一个新文件。项目提供三种用法："),
("bullet", "**硬字幕**：字幕真正“印”进每一帧画面，任何播放器都能看到；"),
("bullet", "**软字幕**：字幕作为独立轨道内嵌进视频文件，播放时可以开关（mov_text 格式）；"),
("bullet", "**实时预览**：播放视频时先预览效果，满意了再真正合成。"),
("p", "实现上有两条渲染路径："),
("bullet", "**近似模式（快）**：把字幕写成 ASS 文件，交给 FFmpeg 的 `ass` 滤镜处理，速度和效果平衡；"),
("bullet", "**精确模式（精）**：逐帧用 Flutter 离屏渲染把字幕画成 PNG 图片（支持高分辨率超采样），再用 FFmpeg 的 overlay 滤镜贴到视频上。精确但慢，适合对字幕样式要求极高的场景；"),
("p", "合成流程是一个状态机：探测视频信息 → 加载字幕 → 渲染 → 执行 → 校验 → 清理中间文件。输出参数很专业（H.264 编码、CRF 20 质量、yuv420p 兼容色深、faststart 让视频边下载边播）。"),

("h2", "4.8 AI 语音转文字：让机器听懂人话"),
("p", "这个功能把视频的语音转成文字字幕，实现方式是调用 B 站必剪（BiliCut）的语音识别接口。流程："),
("num", "**抽取音频**：用 FFmpeg 把视频的音频单独抽出来；"),
("num", "**申请上传授权**：向必剪接口申请一个上传凭证；"),
("num", "**分块上传**：音频可能很大，分块上传，每块带 ETag（一种校验标记）确保完整；"),
("num", "**提交并轮询**：提交后，接口异步处理，程序每隔 2 秒查一次进度，最多轮询 240 次（8 分钟），超时标记失败；"),
("num", "**生成字幕**：拿到识别文本和时间点，生成 SRT 字幕文件，登记进媒体库的字幕资产。"),
("p", "任务管理同样考究：八状态状态机（空闲→抽取→上传→转写中→完成/出错）、每任务 30 分钟超时、任务队列持久化、支持取消（取消信号一路传到网络请求和 FFmpeg）。"),
("note", "有一个很“真实”的工程教训：这个接口是 B 站未公开的私有接口，随时可能失效。代码里留了注释说这是“无鉴权私有接口”。面试时讲“我认识到依赖第三方接口有风险，会做降级和超时处理”，比吹嘘功能更显成熟。"),

("h2", "4.9 OCR 识别硬字幕：让机器“看”字幕"),
("p", "有些视频（特别是老电影、字幕组压制的视频）字幕是“印”在画面里的，没有字幕文件。这个功能用 **OCR（光学字符识别）** 把画面里的文字认出来，生成字幕文件。"),
("p", "技术栈是 PaddleOCR（百度开源）的 PP-OCRv4 模型，模型文件（约 63MB）打包在 App 里，用 ONNX Runtime 在本地运行——**不需要联网，隐私安全**。"),
("p", "完整流程非常精彩："),
("num", "**框选区域**：让用户框出字幕通常出现的画面区域（比如屏幕下方 1/4）；"),
("num", "**抽帧**：用 FFmpeg 按低帧率（每秒 10 帧）抽帧，先去重（mpdecimate 过滤几乎相同的帧），再用 showinfo 记录每帧的时间点；"),
("num", "**识别**：把每帧图片送进神经网络，识别文字和置信度。这一识别在独立 Isolate 里跑，不卡界面；"),
("num", "**变化检测**：用灰度签名比较相邻帧画面是否变化（SSIM 思想），没变化的帧跳过，省算力；"),
("num", "**防闪烁**：连续两帧识别文字相似度足够高（编辑距离 ≥ 0.78）才确认，避免字幕闪烁；"),
("num", "**样式学习**：自动学习字幕的常见行高、位置，过滤水印等常驻文字；"),
("num", "**合并时间轴**：把相同内容的帧合并成一条字幕，时间点连续；"),
("num", "**导出 SRT**：生成标准字幕文件并登记进媒体库。"),
("p", "这里用到了两个算法：**SSIM 变化检测**（判断两幅图像是否相似）和**编辑距离/Levenshtein**（判断两段文字是否相似），都是计算机视觉和文本处理的经典算法，第五节细讲。"),
("note", "同时准备了“CPU 与硬件加速双引擎”：启动时都试一遍，谁快用谁，Windows 上还能用 DirectML 加速。这种“实测择优”的思路很工程化。"),

("h2", "4.10 系统集成：通知栏、锁屏、快捷键、手势"),
("p", "一个“能用”的播放器还需要和操作系统深度配合："),
("bullet", "**通知栏媒体控制**（Android/iOS）：通过 `audio_service` 插件，在通知栏显示封面、进度条、播放/暂停按钮，支持锁屏控制、耳机按键——就像系统自带的音乐 App。800 毫秒节流更新，防止频繁刷新通知；"),
("bullet", "**亮屏保持**：播放时屏幕不自动熄灭（`wakelock_plus`），但做了“多理由协调”机制——下载任务需要亮屏、播放需要亮屏，谁在用就保持，谁都不用了就释放；"),
("bullet", "**手势体系**：双击中央播放/暂停、双击左右两侧快进快退（可设置 5 秒/10 秒）、长按临时倍速、上下滑动调节亮度/音量、桌面滚轮调音量；"),
("bullet", "**桌面快捷键**：18 个播放器快捷键（空格暂停、方向键快进、F 全屏、M 静音……）；"),
("bullet", "**拖拽导入**：Windows/macOS 上把视频文件拖进窗口直接导入；"),
("bullet", "**系统分享接入**：手机上从其他 App“分享视频到 Fluent Player”，直接导入。"),
("p", "这些功能看着琐碎，但每一个都是“打通 App 与系统”的桥梁，背后是平台通道、生命周期管理、权限处理。面试时点到即止即可，重点讲手势判定（自实现双击 300ms 判定）、节流、亮屏协调这几个亮点。"),

# =====================================================================
# 第五部分
# =====================================================================
("pagebreak",),
("h1", "第五部分　藏在代码里的算法与思维（面试加分项）"),

("p", "这一部分是整份文档的精华。面试官不一定懂你的 App，但他一定懂算法。下面每个算法都配了项目里的真实代码和通俗解释，请你至少把前六个彻底看懂。"),

("h2", "5.1 二分查找：一秒定位当前字幕"),
("p", "**它解决什么问题**：播放到第 37 分 42 秒时，要从一万条字幕里找到当前应该显示哪条。"),
("p", "**笨办法 vs 聪明办法**：笨办法是从第一条开始逐条比对，运气最差要查一万次；聪明办法是二分查找——因为字幕按时间排好序，先看中间那条：如果当前时间比它早，说明目标在左半边；比它晚，在右半边。每次砍掉一半，一万条最多查 14 次。"),
("p", "项目里的真实代码（`subtitle_timeline_resolver.dart`）："),
("code", """int _lastStartAtOrBeforeMs(int positionMs) {
  int low = 0, high = _startTimesMs.length - 1;
  int answer = -1;
  while (low <= high) {
    final int mid = (low + high) >> 1;   // 取中间
    if (_startTimesMs[mid] <= positionMs) {
      answer = mid;        // 中间值满足，记下并向右找
      low = mid + 1;
    } else {
      high = mid - 1;      // 否则向左找
    }
  }
  return answer;
}"""),
("p", "**面试怎么讲**：\"字幕定位用到了二分查找：字幕按时间排序，每次取中点和当前播放时间比较，一次砍掉一半，从 O(n) 降到 O(log n)。\"这个 `O(n)` vs `O(log n)` 的说法就是算法复杂度思维。"),

("h2", "5.2 LRU 缓存：缩略图的内存管家"),
("p", "**它解决什么问题**：缩略图加载很贵（要解码视频抽帧），但内存有限，不能全缓存。"),
("p", "**LRU（最近最少使用）**：当缓存满了，把“最久没被使用”的赶出去。就像宿舍书架：最常翻的书放最外面，很久不碰的书挪到最里面，书架满了先扔最里面的。"),
("p", "项目实现用 Dart 自带的 `LinkedHashMap`，开启 `accessOrder`（按访问顺序）后，每次访问一个元素就把它挪到队尾，队头自然就是最久没用的，满了就删队头："),
("code", """// thumbnail_cache_service.dart
final LinkedHashMap<String, ImageProvider> _memoryCache =
    LinkedHashMap<String, ImageProvider>(); // accessOrder 开启后即 LRU
// 访问时先移除再放回尾部，更新“最近使用”顺序
_memoryCache.remove(videoId);
_memoryCache[videoId] = provider;"""),
("p", "**面试怎么讲**：\"缩略图用内存+磁盘两级缓存，内存层用 LRU 策略限制条目数，防止滚动媒体库时内存无限增长；还配合可视区预加载，滚动才不卡。\""),

("h2", "5.3 防抖（Debounce）：别让保存太频繁"),
("p", "**它解决什么问题**：用户拖动媒体库排序，每拖一下状态都变了，如果每次都写盘，一秒钟可能写十几次；但拖完肯定要保存。"),
("p", "**防抖**：设置一个计时器，操作发生时重置计时器；只有“安静”了指定时间才真正执行。就像电梯门：有人进来就重新计时，没人按门了才关门。"),
("p", "项目里媒体库保存是 20 秒防抖，字幕编辑器是 220 毫秒防抖，B 站任务进度 280 毫秒起步的节流。"),
("p", "**防抖 vs 节流**：防抖是“停手后执行一次”；节流是“每隔固定时间执行一次”。比如下载进度通知用节流（每 300 毫秒最多通知一次），媒体库保存用防抖（停手 20 秒才存）。这两个概念面试极爱考。"),

("h2", "5.4 串行任务队列：排队办事"),
("p", "**它解决什么问题**：很多任务不能同时做——比如写库和写库不能同时进行（会互相覆盖），FFmpeg 合并任务太多会拖垮电脑。"),
("p", "项目里的 `SerialTaskQueue` 用异步链实现：每个新任务都接在前一个任务完成后执行。"),
("code", """// serial_task_queue.dart（完整实现，就这么几行）
class SerialTaskQueue {
  Future<void> _tail = Future<void>.value();
  Future<T> enqueue<T>(Future<T> Function() task) {
    final operation = _tail.then<T>((_) => task()); // 接在队尾之后
    _tail = operation.then<void>((_) {}, onError: (_, __) {});
    return operation;
  }
}"""),
("p", "注意那个 `onError`：前一个任务失败，队列不会“中毒”，后面的任务照常执行。**失败不阻塞队列**是它的核心设计。"),

("h2", "5.5 竞态防护：用“代际计数”防止乱套"),
("p", "**它解决什么问题**：异步世界里，你发起请求 A，结果还没回来，又发起了请求 B。A 的结果晚到，如果照常执行，就用旧结果覆盖了新状态——这叫“竞态条件（race condition）”，是并发编程最臭名昭著的 bug。"),
("p", "**代际计数**：每次发起新请求，计数器 +1。每个异步回调回来时，先核对“我的编号还等于当前计数器吗？”不等于，说明已经被更新的请求取代，直接丢弃。"),
("p", "项目里这个模式用到了极致：`_playRequestId`（播放）、`_seekRequestId`（快进）、`_subtitleLoadRequestId`（字幕加载）、`_preloadedRequestId`（预加载）……还有字幕加载的 `_subtitleRefreshToken`、弹幕的 `_loadToken`，全是同一个思想。"),
("code", """// media_playback_service.dart 里的校验
bool _isCurrentPlayRequest(int requestId, String itemId) {
  // 编号不是最新的？当前播的不是这个视频？→ 过期结果，丢弃
  if (requestId != _playRequestId || _currentItem?.id != itemId) {
    return false;
  }
  return true;
}"""),
("p", "**面试怎么讲**：\"播放器里异步操作极多，我用代际计数（每发一次请求就 +1，回调先校验编号）防止过期结果覆盖新状态，彻底解决了播放、快进、字幕加载之间的竞态问题。\"这是大厂面试官最爱听的东西。"),

("h2", "5.6 正则表达式：从杂乱文本里提取信息"),
("p", "**它解决什么问题**：用户可能粘贴“快看这个视频 https://www.bilibili.com/video/BV1xx411c7mD 太好笑了”这样一整句话，程序要从中抠出视频 ID。"),
("p", "**正则表达式**：一种“模式匹配”语言，描述“我要找什么样的字符串”。比如："),
("code", """// bilibili_url_parser.dart
static const String _bvPattern = r'(BV[a-zA-Z0-9]{10})';
// 解释：BV 开头 + 10 个字母或数字 → 这就是 B 站视频 ID"""),
("p", "正则的常用符号：`\\d` 数字、`\\w` 字母数字下划线、`+` 一个或多个、`{10}` 恰好 10 个、`( )` 分组捕获、`^$` 开头结尾。你项目里还有从 YouTube 分享文本里提取链接、从弹幕 XML 里提取每条弹幕等大量应用。"),
("p", "**面试怎么讲**：\"用户输入的可能是整段分享文案，我用正则提取出 BV 号/av 号/短链，再区分普通视频和番剧。\""),

("h2", "5.7 哈希与指纹：认出同一个视频"),
("p", "**它解决什么问题**：要判断“这个视频需不需要转一个兼容音频副本”，需要一个能识别“同一个文件”的标记。"),
("p", "**哈希（Hash）**：把任意内容算成一串固定长度的数字（指纹）。同样的内容算出的指纹一定相同，内容变一点指纹就大变。项目里："),
("bullet", "给“文件路径+大小+修改时间”算 SHA-256，得到兼容副本的文件名，同名即同源；"),
("bullet", "给参数算 MD5，用于 B 站 WBI 签名；"),
("bullet" ,"给 OCR 模型文件算 SHA-256 校验完整性。"),
("p", "**面试怎么讲**：\"我用 SHA-256 给源文件生成指纹来命名转码副本，实现幂等——同一源文件只转一次，不会重复转码。\"这个词“幂等”（idempotent）说出来，面试官会觉得你很有水平。"),

("h2", "5.8 状态机：每个任务都有“剧本”"),
("p", "**它解决什么问题**：下载、转写、合成这种长任务，如果不限制流程，可能出现“没下载完就合并”“没合并就完成”的乱象。"),
("p", "**状态机**：明确规定任务只能在“剧本”允许的状态之间切换。比如 B 站下载任务的状态机："),
("code", """enum DownloadStatus {
  pending, queued, fetchingInfo, downloading,
  merging, checking, repairing, completed, failed
}
// 合法流向：pending → queued → fetchingInfo → downloading
//         → merging → checking → completed
//         任一步失败 → failed，可重试回到 queued"""),
("p", "项目里还有播放状态机（idle/loading/playing/paused/error）、转写状态机（八态）、合成状态机（queued→preparing→rendering→finalizing→completed/failed）。"),
("p", "**面试怎么讲**：\"下载任务我用状态机管理，每个状态有明确的入口和出口，重启后还能根据持久化状态正确恢复任务。\"状态机在编译器、游戏、通信等领域无所不在，是非常基础的抽象。"),

("h2", "5.9 插值与漂移校正：让弹幕不抖动"),
("p", "**它解决什么问题**：底层播放器上报的播放位置偶尔会轻微抖动（比如突然回退 30 毫秒又前进），如果直接显示，进度条和弹幕就会肉眼可见地“跳”。"),
("p", "**做法**（`playback_timeline_clock.dart`）：界面自己维护一个“推算时钟”。每帧按播放速度推算位置（插值），保证平滑；底层上报的位置只用来做“漂移校正”——如果持续有小偏差，就微调推算速度去追平，每次最多调 0.5%，肉眼完全察觉不到；偏差很大（超过 100 毫秒）就当成用户拖进度条，重置时钟。"),
("p", "**面试怎么讲**：\"弹幕渲染需要平滑时间轴，但底层位置采样有抖动。我用插值维护推算时钟保证平滑，用限幅的漂移校正保证长时间准确，大偏差则识别为跳转直接重置。\"这段表述绝对能打动面试官。"),

("h2", "5.10 泳道分配：弹幕不重叠的秘诀"),
("p", "**它解决什么问题**：同一时刻可能冒出几十条滚动弹幕，都放在同一行会叠成黑块。"),
("p", "**做法**：屏幕纵向分成 13 条“泳道”（车道）。每条弹幕到达时，找一个“空闲”的泳道放进去——怎么判断空闲？看这条泳道上一条弹幕是否已经滚过去了。"),
("code", """// bilibili_danmaku_ass.dart 的泳道分配器（简化）
class _LaneAllocator {
  static const int laneCount = 13;
  final scroll = List<double>.filled(laneCount, 0);
  int allocate({required double start, required double duration}) {
    for (var i = 0; i < lanes.length; i++) {
      if (start >= lanes[i]) {   // 这条泳道此刻空了
        lanes[i] = start + duration; // 记录占用到什么时候
        return i;
      }
    }
    return -1; // 全满，丢弃这条弹幕
  }
}"""),
("p", "注意一个聪明的细节：滚动弹幕的“占用时间”还考虑了文字长度——文字越长滚得越久，占用时间就越长。这里用了一个估算公式，把速度、宽度、长度都算进去了。这就是“贪心 + 启发式”的调度思想。"),

("h2", "5.11 编辑距离（Levenshtein）：判断两句话像不像"),
("p", "**它解决什么问题**：OCR 连续识别两帧，字幕文字有一点抖动（比如“你好”和“你 好”），要判断它们是不是同一条字幕；合并时间轴时，判断两条相邻识别结果是否内容重复。"),
("p", "**编辑距离**：把一句话变成另一句话，最少需要几步操作（增、删、改一个字符）。比如“你真好”变成“你好”：删掉一个“真”，距离是 1；距离越小越相似。"),
("p", "项目里：连续两帧识别结果相似度 ≥ 0.78 才算“稳定”，时间轴合并时相似度 ≥ 0.82 才合并。这个“相似度阈值”参数，就是调出来的工程经验值。"),
("p", "**面试怎么讲**：\"OCR 防闪烁和去重用到了编辑距离算法，通过相似度阈值判断两帧是不是同一句字幕。\""),

("h2", "5.12 编码探测：猜猜字幕文件是什么编码"),
("p", "**它解决什么问题**：同一个“你好”，用 UTF-8 存是一串字节，用 GBK 存是另一串字节，读错就乱码。"),
("p", "**做法**（`subtitle_parser.dart` 的 `decodeBytes`）：按优先级试探——"),
("bullet", "看文件头的 BOM 标记（FF FE 是 UTF-16LE，EF BB BF 是 UTF-8）；"),
("bullet", "没有标记就数“零字节”：UTF-16 编码里每个汉字会夹带 0 字节，统计前 200 字节里 0 出现在奇数位还是偶数位，就能推断字节序；"),
("bullet", "再依次试 UTF-8、GBK，每轮都验证“解出来像不像字幕”（是否包含 `-->` 时间标记等特征）；"),
("p", "这种“多个假设依次验证，选最合理者”的思路，叫**启发式探测**。很多解码器、语音识别、字符编码检测都是这个套路。"),

# =====================================================================
# 第六部分
# =====================================================================
("pagebreak",),
("h1", "第六部分　从项目里学到的软件开发素养"),

("h2", "6.1 设计模式：前辈总结的“套路”"),
("p", "设计模式是前辈程序员总结出来的常见问题解法，就像菜谱里的“红烧”技巧。你的项目里大量运用："),
("table", ["设计模式", "通俗解释", "项目里的例子"],
 [
  ["单例（Singleton）", "全局只造一个对象", "MediaPlaybackService、LibraryService 等十几个服务"],
  ["工厂（Factory）", "用统一入口创建对象", "上面单例的 factory 构造、YtDlpBinaryInstaller 解包二进制"],
  ["观察者（Observer）", "一个变了，通知所有关注者", "ChangeNotifier + notifyListeners，播放状态变了界面自动刷新"],
  ["适配器（Adapter）", "把 A 的接口翻译成 B 能用的", "把 media_kit 包装成 video_player 的底层实现"],
  ["门面（Facade）", "把复杂子系统藏在一个简单入口后面", "VideoComposeManager 统一调度 13 个合成子服务"],
  ["依赖注入（DI）", "把需要的“零件”从外面塞进来", "各服务的 initialize({required ...}) 注入其他服务"],
 ]),
("p", "面试被问“了解哪些设计模式”，你报出这几个并各配一个项目例子，就是满分答案。"),

("h2", "6.2 依赖注入与解耦"),
("p", "**依赖注入**通俗讲：我不自己“造”我需要的工具，而是让别人把工具递给我。比如 `MediaPlaybackService.initialize()` 需要播放列表、进度追踪、媒体库、字幕服务，main.dart 在启动时把它们传进去。"),
("p", "好处是：各部分可以独立替换、独立测试，改播放列表不影响播放服务。这也解释了为什么项目能交给 AI 分模块迭代——边界清楚，互不干扰。"),

("h2", "6.3 数据迁移：老用户的数据不能丢"),
("p", "软件一直在升级，但老用户的数据格式是“旧”的。项目对每个 `fromJson`（从磁盘读数据）都做了兼容处理："),
("bullet", "新字段读取时带默认值：`json['durationMs'] as int? ?? 0`，老数据没有这个字段就填 0；"),
("bullet", "旧字段自动迁移：比如旧的 `extraSubtitles` 迁移到新结构，旧字幕样式迁移到新的三段式样式；"),
("bullet", "所有持久化字段保持向后兼容——新增字段绝不影响老数据读取。"),
("p", "这是**成熟工程师和业余爱好者的分水岭**。很多新手的 App 一改数据格式，老用户数据全丢。你可以骄傲地在面试里讲这一点。"),

("h2", "6.4 错误处理：App 不能动不动就崩"),
("p", "项目里的错误处理是成体系的："),
("bullet", "**全局兜底**：`FlutterError.onError` 捕获界面渲染错误，`runZonedGuarded` 捕获未捕获异常，都写进 `crash_log.txt`（限制 1MB 防无限增长）；"),
("bullet", "**服务初始化容错**：每个服务 `init()` 都用 try/catch 包住，失败只打日志，不影响其他服务；"),
("bullet", "**用户可见的错误**：用统一的 `AppToast` 弹提示（分 info/error 等类型），不直接抛界面错误；"),
("bullet", "**静默失败也留痕**：虽然有些地方 `catch (_) {}` 静默吞掉异常（这是项目的一个已知缺点，面试如果被问到可以大方承认并说改进方向），但大多数错误路径至少 `debugPrint` 留日志。"),
("p", "面试问“你怎么保证 App 不崩”：\"我会分层处理：全局兜底捕获 + 服务级容错 + 用户可见的 Toast 提示，并且有崩溃日志便于复盘。\""),

("h2", "6.5 异步编程与性能优化"),
("p", "一个流畅的 App，秘诀是**把重活放到后台**、**把轻活留在主线程**："),
("bullet", "视频解码 → 播放引擎后台线程；"),
("bullet", "字幕解析、弹幕解析 → `compute`（后台线程）或 Isolate；"),
("bullet", "文件扫描、压缩包解压 → 后台 Isolate；"),
("bullet", "缩略图 → 磁盘 + 内存两级缓存 + 预加载；"),
("bullet", "通知栏更新 → 800 毫秒节流；"),
("bullet", "下载进度通知 → 250~1200 毫秒自适应节流；"),
("bullet", "保存文件 → 防抖 + 串行队列。"),
("p", "这些手段的本质都是：**CPU 和内存是稀缺资源，要花在刀刃上**。"),

("h2", "6.6 Isolate：给主线程配个“二把手”"),
("p", "Dart 是单线程语言——所有代码默认挤在同一条“车道”上。车道堵了，界面就卡。解决办法是 **Isolate**：另开一条完全独立的“车道”（独立内存、独立线程），把重活派过去，做完把结果传回来。"),
("p", "项目里 OCR 就是典型：`Isolate.spawn(_ocrWorkerMain, ...)` 开一个后台工作线程，里面加载 ONNX 模型、跑神经网络。主线程通过“请求 ID + Completer”等结果，完全不卡。"),
("p", "这是把复杂 AI 能力塞进 App 的关键工程手段，面试值得讲。"),

("h2", "6.7 测试：给代码上保险"),
("p", "90 个测试文件覆盖了项目最核心的纯逻辑：URL 解析、字幕解析、弹幕泳道、任务队列、数据迁移、状态机……",),
("p", "测试的价值：改一个功能，跑一遍测试就知道有没有改坏别的地方。项目里还用了 `glados` 库做**属性测试**——不是给固定输入测固定输出，而是随机生成大量输入，验证“性质”始终成立（比如“排序后第一个元素一定是最小的”）。"),
("p", "面试被问“你怎么保证代码质量”，回答：\"我写了很多针对解析器、状态机、迁移逻辑的单元测试，改代码后跑测试回归。\""),

("h2", "6.8 工程规范：为什么代码要整齐"),
("p", "12 万行的项目不乱，靠的是一套约定："),
("bullet", "**命名即文档**：文件后缀表明职责（`*_service.dart` 服务、`*_parser.dart` 解析器），类名大写驼峰、私有成员下划线开头；"),
("bullet", "**注释讲“为什么”**：项目注释大量解释“为什么这么写”，而不是“写了什么”——后者看代码就知道；"),
("bullet", "**平台分支集中**：桌面/移动的差异在入口统一分流，不让平台判断散落各处；"),
("bullet", "**约定红线**：比如“不许绕过 video_player 直接调 media_kit”“不许向 lib/ 外写业务数据”。"),
("p", "这种“约束即自由”的工程规范意识，是专业开发团队的标配。"),

# =====================================================================
# 第七部分
# =====================================================================
("pagebreak",),
("h1", "第七部分　面试准备：把项目讲成故事"),

("h2", "7.1 60 秒电梯演讲（开场白）"),
("p", "面试官让你介绍项目时，不要背功能清单，讲一个“故事”。参考模板："),
("quote", "\"我做一个跨平台的视频播放器，支持 Windows、macOS、Android、iOS。它最大的特点是除了播放，还集成了字幕翻译、弹幕渲染、B 站和 YouTube 下载、语音转文字、OCR 识别硬字幕、视频合成这些能力。项目约 12 万行代码，90 个测试。\n\n技术上我最自豪的是三件事：第一，我自己实现了字幕解析、弹幕泳道分配、字幕时间轴二分查找这些算法；第二，我用代际计数解决了播放器里大量的异步竞态问题；第三，我把本地的 ONNX 模型跑在独立 Isolate 里做 OCR，界面完全不卡。\n\n因为我不懂代码，整个项目是和 AI 协作完成的，但每一个模块我都反复理解过，而且写了很多测试来验证正确性。\""),
("p", "最后一段“诚实说明 AI 协作”非常重要——现在用 AI 写代码很普遍，坦诚反而加分，重要的是展示你对代码的**理解**和**验证**。"),

("h2", "7.2 面试官最爱问的 10 个问题与参考回答"),
("p", "下面每个问题的回答都基于你项目的真实情况，可以直接背。"),

("h3", "问题 1：介绍一下你的项目"),
("p", "用 7.1 的电梯演讲。讲完主动补一句：\"您想听哪一部分的细节？播放器架构、下载模块，还是 AI 相关？\"把面试官引入你最熟的领域。"),

("h3", "问题 2：为什么用 Flutter？它和原生有什么区别？"),
("p", "回答要点：Flutter 是 Google 的跨平台 UI 框架，用 Dart 语言，一套代码编译到 Android/iOS/Windows/macOS。底层用 Skia/Impeller 引擎自己绘制界面，不依赖系统控件，所以跨平台表现一致。和原生比，开发效率高、界面一致，缺点是包体积更大、个别底层能力需要写原生代码配合。我的项目就是通过 MethodChannel 调用原生能力（运行 yt-dlp 进程、系统媒体控制）。"),

("h3", "问题 3：你的播放器是怎么实现跨平台播放的？"),
("p", "回答要点：我基于 media_kit（它底层是 libmpv + FFmpeg）。我写了一个适配器，把 media_kit 注册成 video_player 的底层实现，这样上层用标准接口，底层换成了解码能力更强的引擎，所以 MKV、各种编码都能放，而且四个平台统一。这还用到了适配器设计模式。"),

("h3", "问题 4：怎么处理异步竞态？"),
("p", "回答要点：播放器里有大量异步操作（播放、快进、字幕加载、预加载下一集）。我用代际计数——每次发起操作计数器 +1，异步结果回来先校验编号，过期的直接丢弃。快进后还有两段式校验（180 毫秒、650 毫秒）确认落点，防止 UI 与底层不同步。"),

("h3", "问题 5：弹幕是怎么实现的？"),
("p", "回答要点：分三步。第一步解析 B 站 XML，提取时间/类型/颜色/文字；第二步用泳道分配算法把同时出现的弹幕分到不同行，避免重叠；第三步渲染时把常用文字预渲染成纹理存进图集，按 LRU 管理，再用性能调速器监测帧耗时自动降级。"),

("h3", "问题 6：B 站下载是怎么实现的？"),
("p", "回答要点：自己实现，不调现成工具。流程是：正则提取链接 ID → 调接口拿视频信息 → 用官方 WBI 签名算法给取流参数签名 → 支持扫码/Cookie 登录 → DASH 音视频分片下载、HTTP Range 断点续传 → FFmpeg 合并 → 时长校验，不一致自动转码修复 → 下载弹幕 → 导入媒体库。整个流程有状态机、串行队列、看门狗超时。"),

("h3", "问题 7：OCR 是怎么做的？"),
("p", "回答要点：本地跑 PaddleOCR PP-OCRv4 的 ONNX 模型，不联网。用 FFmpeg 按低帧率抽帧 + 去重，用户框选字幕区域，每帧送进神经网络识别；用灰度变化检测跳过重复帧，用编辑距离判断相邻帧是否同一句字幕（防闪烁），再用相似度阈值合并时间轴，最后导出 SRT。推理跑在独立 Isolate，不卡界面。"),

("h3", "问题 8：语音转文字怎么做的？"),
("p", "回答要点：调用 B 站必剪的 ASR 接口。流程是 FFmpeg 抽音频 → 申请上传授权 → 分块上传（带 ETag）→ 提交任务 → 每 2 秒轮询进度 → 拿到结果生成 SRT。任务是状态机管理，支持取消和断点恢复。我在这里也认识到依赖第三方私有接口有失效风险，所以做了超时和失败处理。"),

("h3", "问题 9：项目里最难的地方是什么？"),
("p", "回答要点：对我个人来说难，对项目本身来说复杂。建议这样说：\"对我来说最难的其实是理解，因为我是和 AI 协作开发的。但从技术角度，播放器的时序问题最难——播放、快进、字幕、弹幕都要对同一个时间轴负责，任何抖动用户都看得见。我通过自研时间线时钟（插值 + 限幅漂移校正）和代际计数竞态防护解决了。\""),

("h3", "问题 10：这个项目你怎么验证它是对的？"),
("p", "回答要点：\"我写了 90 个测试文件，重点覆盖解析器、弹幕泳道、任务队列、数据迁移这些纯逻辑，还用了属性测试随机验证不变量。另外整个 App 在我自己的 Windows 和 Android 设备上反复跑过。\""),

("h2", "7.3 讲项目的高级技巧"),
("bullet", "**先总后分**：永远先一句话给全貌，再展开细节，防止面试官跟丢；"),
("bullet", "**一个功能三句话**：解决什么问题 → 大致思路 → 用到的算法/设计模式。控制在一分钟以内，留出被追问的空间；"),
("bullet", "**主动暴露弱点并给改进方案**：比如\"我们的 OCR 模型只有 63MB，识别繁体效果一般，改进方向是用更大模型 + 服务器端推理\"。展示批判性思维比炫耀功能更受面试官欣赏；"),
("bullet", "**多用专业词汇但要解释**：说\"二分查找\"同时说\"每次砍掉一半\"，证明你懂，而不是背词；"),
("bullet", "**准备一张“架构图”**：可以手绘 UI 层→服务层→模型层→外部世界的分层图，面试时画出来，立刻显得专业。"),

("h2", "7.4 诚实与边界：AI 写的代码怎么讲"),
("p", "你完全不懂代码，这个事实不需要隐瞒——面试官一眼能看出来。但你有两个别人没有的优势："),
("bullet", "**你有一个 12 万行、功能完整的真实项目**，很多人学完四年编程也没有；"),
("bullet", "**你把它彻底搞懂的过程**，本身就是学习能力的证明。"),
("p", "建议的话术：\"我的项目是 AI 辅助开发的。正因为我不是科班出身，我对每一行代码都做了更仔细的理解——我花了很多时间读源码、写文档、跑测试来确认它是对的。这个过程让我补上了很多编程基础。\"然后立刻展示你懂的细节（用本文档第五部分的内容）。"),
("p", "**面试官真正想知道的不是“代码是不是你写的”，而是“你会不会编程、能不能学习、有没有工程思维”**。这三点，本文档已经帮你准备好了。"),

# =====================================================================
# 结语
# =====================================================================
("pagebreak",),
("h1", "结语　下一步怎么学"),

("h2", "给你的学习路线建议"),
("p", "拿到拔尖班名额只是第一步。这个项目已经是你的“富矿”，建议按下面的顺序继续挖："),
("num", "**先读懂本文档第二部分到第四部分**，能对着代码指认每个文件干什么；"),
("num", "**精读第五部分算法**，每个算法自己拿纸笔跑一遍小例子（比如给 10 个数手算二分查找）；"),
("num", "**在项目里做一次小改动**，比如给播放器加一个快捷键、改一个字幕默认字号——体会“改一行代码 → 跑起来看到效果”的闭环；"),
("num", "**系统学习 Dart 语法**（网上有免费教程，一两天过一遍），配合项目代码理解；"),
("num", "**学数据结构与算法**（这是计算机专业的核心课）：数组、链表、树、栈、队列、排序、二分、动态规划，每一章都可以在项目里找到对应物；"),
("num", "**学操作系统和网络基础**：进程与线程（对应 Isolate）、文件系统（对应持久化）、HTTP（对应下载模块）、并发（对应竞态防护）；"),
("num", "**把项目当实验田**：每学一个新概念，就回项目里找它在哪里。找不到就自己加一个。"),
("p", "最后记住一句话：**计算机科学不是背会的，是做会的**。你现在有一个比绝大多数初学者都大的真实项目，这已经是最大的优势。剩下的，只是时间。"),

("p", "—— 文档完 ——"),
]
