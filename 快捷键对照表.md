# 快捷键对照表

单键触发，不用 Ctrl / Alt / Fn。输入框聚焦时不拦截字母（链接输入框里 `Enter` 仍触发解析）。同一功能跨页同键。

## 跨页同键

| 键 | 各页含义 |
|----|----------|
| `Esc` | 返回 / 关弹层 / 退出管理 |
| `S` | 设置（媒体库搜索除外） |
| `A` | 全选 |
| `R` | 移除 / 删除所选 / 回收站 |
| `I` | 导入 |
| `E` | 导出 |
| `B` | 批量管理 |
| `N` | 新建 |
| `Space` | 开始 / 暂停 |
| `P` | 暂停（下载页） |
| `V` | 粘贴（下载页，对标 Ctrl+V） |
| `F` | 从文件选 / 全屏（视页面） |
| `X` | FluentPack / 导入与导出页 / 清空输入框（视页面） |
| `C` | 字幕相关 / 取消（视页面） |

## 全局

| 键 | 功能 |
|----|------|
| `F11` | 窗口全屏 |
| `Esc` | 已全屏时退出窗口全屏 |
| `Ctrl+R` | 刷新应用 |

## 媒体库（首页 / 文件夹）

### 普通模式

| 键 | 功能 |
|----|------|
| `Esc` | 返回上一级 / 返回搜索结果 |
| `S` | 搜索媒体库 |
| `P` | 媒体库设置 |
| `V` | 列表 / 卡片视图 |
| `F` | 窗口全屏 |
| `B` | 进入批量选择 |
| `R` | 打开回收站 |
| `T` | 调整卡片样式 |
| `E` | 导出设置 |
| `O` | 大文件目录（仅 Windows） |
| `Z` | 定时关闭 |
| `N` | 新建合集 |
| `I` | 导入视频或音频 |
| `D` | B站视频下载 |
| `Y` | YT-DLP 下载 |
| `C` | 批量字幕生成 |
| `M` | 批量导入 |
| `X` | 打开导入与导出 |
| `Space` `←` `→` | 后台迷你条：播放 / 快退 / 快进 |

FAB 收起时，`N I D Y C M` 仍直接打开对应功能。

### 选择模式

| 键 | 功能 |
|----|------|
| `Esc` | 退出选择 |
| `A` | 全选 / 取消全选 |
| `R` | 移入回收站 |
| `N` | 重命名（仅选中 1 项） |
| `U` | 移动到上一级（仅文件夹） |
| `X` | FluentPack 导出（已有选中项） |
| `V` `F` `Z` | 仍可用：视图 / 全屏 / 定时关闭 |
| `Space` `←` `→` | 仍可控制迷你条 |

根目录按 `U`、多选时按 `N`：吃键、不做事。选择态未选中任何项时按 `X`：吃键、不做事。

### 导入视频或音频（弹层）

媒体库按 `I` 打开后：

| 键 | 功能 |
|----|------|
| `Esc` | 关闭弹层 |
| `A` | 从相册导入（仅移动端） |
| `F` | 从文件管理导入 |
| `B` | 导入 Bilibili 链接（在线播放） |
| `Z` | 从文件管理导入压缩包 |
| `O` | 从文件管理导入文件夹（非 iOS） |
| `X` | 从 FluentPack 文件导入 |

文案直接带键，如 `从文件管理导入 (F)`。

## 视频播放器

| 键 | 功能 |
|----|------|
| `Esc` | 退出播放 |
| `Space` | 播放 / 暂停 |
| `←` / `→` | 快退 / 快进 |
| `↑` / `↓` | 音量加减 |
| `[` / `]` | 倍速减 / 加一档 |
| `Shift+←` / `Shift+→` | 同上（备用） |
| `M` | 静音 |
| `F` | 窗口全屏 |
| `P` | 上一集 |
| `N` | 下一集 |
| `G` | 选集 |
| `H` | 章节 |
| `C` | 字幕开 / 关 |
| `L` | 字幕库 |
| `E` | 字幕编辑 |
| `T` | 悬浮字幕设置 |
| `B` | 字幕边栏 |
| `V` | 合成视频 |
| `O` | OCR 字幕 |
| `J` | 移动字幕 |
| `D` | 弹幕开 / 关 |
| `X` | 弹幕设置 |
| `S` | 播放器设置 |
| `A` | 画面比例 |
| `K` | 锁定 / 解锁 |
| `Q` | 清晰度 |
| `R` | 还原屏幕 |
| `Z` | 定时关闭 |

锁屏时只响应 `K` 和音量键。无对应按钮时吃键、不做事。刻意空出：`W`（哔哩哔哩宽屏）、`I`（YouTube 迷你播放器）。

## B站下载 / B站在线导入

同一屏，双击标题切换。两套底栏共用键，没有的按钮按了不做事。

| 键 | 功能 |
|----|------|
| `Esc` | 返回 |
| `W` | 切换下载 / 在线导入 |
| `Enter` | 解析（输入框内同样） |
| `V` | 粘贴 |
| `X` | 清空输入框 |
| `S` | 下载设置 / 解析设置 |
| `L` | 登录 / Cookie |
| `A` | 全选 |
| `Space` | 下载并合并（下载）/ 导出到媒体库（在线） |
| `P` | 暂停下载（仅下载） |
| `I` | 导入到媒体库（仅下载） |
| `E` | 导出到媒体库（仅在线；下载模式与 `I` 同义） |
| `R` | 移除所选 |

不配键：清空全部任务、保持亮屏、任务行更多菜单。换行只用换行按钮。

## YT-DLP 下载

与 B 站下载页同一套输入/设置键，底栏多几个任务键。

| 键 | 功能 |
|----|------|
| `Esc` | 返回 |
| `Enter` | 解析 |
| `V` | 粘贴 |
| `X` | 清空输入框 |
| `S` | 下载设置 |
| `A` | 全选 |
| `Space` | 开始 |
| `P` | 暂停 |
| `Q` | 插队 |
| `C` | 取消 |
| `T` | 重试 |
| `E` | 导出到媒体库 |
| `R` | 移除所选 |

不配键：清空全部任务、保持亮屏、环境/更新、任务卡展开。

## 批量字幕生成

| 键 | 功能 |
|----|------|
| `Esc` | 返回 |
| `A` | 添加视频 / 选择内部视频 |
| `F` | 选择外部文件 |
| `Space` | 开始全部 / 暂停全部（按钮本身是切换） |
| `D` | 完成后自动移除 / 保留已完成 |
| `S` | 外部视频设置 |
| `C` | 清除已完成 |

不配键：清除全部任务。

## 批量导入

| 键 | 功能 |
|----|------|
| `Esc` | 返回 |
| `I` | 导入媒体 |
| `C` | 导入字幕 |
| `H` | 操作说明 |

## 导入与导出（FluentPack）

| 键 | 功能 |
|----|------|
| `Esc` | 管理态退出管理，否则返回 |
| `E` | 导出 Tab |
| `I` | 导入 Tab |
| `N` | 当前 Tab 主按钮：新建导出 / 选择文件 |
| `B` | 批量管理 |
| `A` | 全选（管理态） |
| `R` | 删除所选（管理态） |

选择要导出的内容：

| 键 | 功能 |
|----|------|
| `Esc` | 返回 |
| `A` | 全选 |
| `F` | 全部展开 / 折叠 |
| `Enter` | 继续 |

搜索框聚焦时字母不拦截；`Enter` 不触发继续。导入预览对话框：`Enter` 开始导入，`Esc` 取消。

## 布局

播放器：

```
Q 清晰度   W        E 字幕编辑  R 还原     T 字幕样式            O OCR    P 上一集
A 画面比例 S 设置    D 弹幕      F 全屏     G 选集     H 章节  J 移动字幕  K 锁定  L 字幕库
Z 定时关闭 X 弹幕设置 C 字幕开关  V 合成     B 字幕边栏  N 下一集 M 静音
```

媒体库：

```
               E 导出设置  R 回收站/移入回收站  T 卡片样式           Y YT-DLP  U 上一级  I 导入  O 大文件  P 设置
A 全选         S 搜索      D B站下载            F 全屏
Z 定时关闭     X 导入与导出/FluentPack导出  C 批量字幕     V 视图     B 批量管理  N 新建/重命名  M 批量导入
```

下载 / 在线导入 / YT-DLP：

```
W 切换模式          E 导出到库              I 导入到库              P 暂停
A 全选    S 设置    D（空）      F         G         H         J         K         L 登录
          X 清空输入 C 取消      V 粘贴     B         N         M
Enter 解析    Space 开始/下载/导出    Q 插队    T 重试    R 移除
```

## 相对旧表的字母改动

- `D`：移动字幕 → 弹幕开关
- 移动字幕改为 `J`
- 其余旧字母保持不变
